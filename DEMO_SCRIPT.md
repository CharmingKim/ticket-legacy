# TicketLegacy 발표 시연 대본
> 총 발표 시간: **2시간 30분 ~ 3시간** 기준  
> 시연 진행자 1명 + 슬라이드 전환 1명 권장  
> 이 파일을 발표 당일 별도 모니터 또는 태블릿에 띄워두고 읽으면서 진행

---

## ⚡ 발표 당일 시작 전 필수 체크리스트

> **시연 30분 전에 반드시 완료**

```
□ MySQL 8 기동 확인
  mysql -uroot -p1234 springgreen6 -e "SELECT COUNT(*) FROM performance"
  → 200+ 출력되면 OK

□ Redis 기동 확인
  redis-cli ping  → PONG

□ 3개 서버 기동 (각각 별도 터미널)
  mvn tomcat7:run -pl ticket-user -am     # :8080  (약 40초)
  mvn tomcat7:run -pl ticket-partner -am  # :8081  (약 40초)
  mvn tomcat7:run -pl ticket-admin -am    # :8082  (약 40초)

□ 브라우저 탭 3개 미리 열어두기
  탭1: http://localhost:8082/admin/login    (어드민)
  탭2: http://localhost:8081/partner/login (파트너)
  탭3: http://localhost:8080/member/login  (유저)

□ 기존 로그인 쿠키 초기화 (중요)
  브라우저 개발자도구 → Application → Cookies → localhost
  USER_TOKEN / PARTNER_TOKEN / ADMIN_TOKEN 전부 삭제

□ 시연용 계정 메모지 준비
  admin@ticketlegacy.com / Cks159753!
  promoter1@test.com     / Cks159753!
  promoter2@test.com     / Cks159753!  (PENDING 계정 — 승인 시연용)
  user1@test.com         / Cks159753!
```

---

## 📋 전체 시연 흐름 (목차)

| 순서 | 섹션 | 소요 시간 | 핵심 포인트 |
|------|------|-----------|-------------|
| 1 | 슬라이드 발표 (파트 1~3) | 약 40분 | 개요·요구사항·아키텍처 |
| 2 | **라이브 시연 1** — 어드민 | 약 20분 | 대시보드·FSM 승인·공연 심사 |
| 3 | **라이브 시연 2** — 파트너 | 약 20분 | 공연 등록·좌석 설정 |
| 4 | **라이브 시연 3** — 사용자 | 약 25분 | 예매·결제·취소 |
| 5 | 슬라이드 발표 (파트 4~5) | 약 25분 | 핵심 기능·데이터 |
| 6 | **라이브 시연 4** — 기술 심화 | 약 15분 | FSM 위반·동시성·코드 설명 |
| 7 | 슬라이드 발표 (파트 6~7) | 약 15분 | 도전·로드맵·결론 |
| 8 | Q&A | 약 20~30분 | 예상 질문 대비 |

---

---

# 1부 — 슬라이드 발표 (파트 1~3)

> 슬라이드 1번부터 진행. 아래 대사는 PPT 대본 슬라이드와 병행 사용.

**[표지 슬라이드]**

> "안녕하세요. TicketLegacy 프로젝트 최종 발표를 시작하겠습니다.
> 
> 저희 팀이 개발한 TicketLegacy는 인터파크, 예스24 티켓과 같은 실제 공연 예매 서비스를 모델로 한 엔터프라이즈급 웹 플랫폼입니다.
>
> 단순히 기능이 동작하는 수준을 넘어서, 실제 운영 환경에서 발생하는 동시성 문제, 보안 이슈, 상태 관리 복잡성을 설계 단계에서 해결하는 것을 목표로 했습니다.
>
> 지금부터 약 2시간 30분 동안 프로젝트 배경과 설계, 라이브 시연, 기술적 도전과 향후 계획까지 순서대로 설명드리겠습니다."

---

**[목차 슬라이드]**

> "발표는 총 7개 파트로 구성되어 있습니다.
>
> 슬라이드 발표와 라이브 시연을 번갈아 진행하면서, 개념 설명 직후에 바로 실제 화면으로 확인하는 방식으로 진행하겠습니다.
>
> 특히 어드민 포털에서의 FSM 상태 관리, 파트너의 공연 등록, 사용자의 좌석 예매 플로우를 직접 시연해 드리겠습니다."

---

**[PART 01 — 프로젝트 개요]**

> "먼저 프로젝트 배경과 목표입니다.
>
> 이 프로젝트를 시작하면서 저희 팀이 가장 먼저 정의한 것은 '무엇을 만드는가'가 아니라 '어떤 문제를 해결하는가'였습니다.
>
> 공연 예매 서비스에서 가장 핵심적인 기술 문제는 두 가지입니다.
>
> 첫 번째. 100명이 동시에 같은 좌석의 '예매' 버튼을 눌렀을 때, 한 명만 성공해야 한다는 것.
> 이것이 제대로 구현되지 않으면 중복 예매가 발생하고, 실제 공연장에서 두 명이 같은 자리에 앉는 상황이 벌어집니다.
>
> 두 번째. 기획사가 등록한 공연이 어드민의 심사 없이 바로 판매되면 안 된다는 것.
> 공연 정보 검증, 부적절한 콘텐츠 차단, 환불 정책 확인 등 심사 단계가 반드시 필요합니다.
>
> 저희는 첫 번째 문제를 Redis Dual Defense로, 두 번째 문제를 FSM으로 해결했습니다. 이 두 기술이 이 프로젝트의 핵심입니다."

---

**[개발 환경 슬라이드]**

> "개발 환경과 기술 제약사항입니다.
>
> Spring Boot가 아닌 Spring Legacy MVC 5.3을 사용했습니다.
> 이 선택에는 두 가지 이유가 있었습니다.
>
> 하나는, 현업의 많은 프로젝트가 아직도 Spring Legacy 기반으로 운영되고 있고, 이를 유지보수하거나 마이그레이션하는 역량이 실제로 필요하기 때문입니다.
>
> 다른 하나는, Spring Boot의 자동 설정에 의존하지 않고 root-context.xml, servlet-context.xml, security-context.xml을 직접 작성하면서, 프레임워크가 내부적으로 어떻게 동작하는지 깊이 이해하기 위해서입니다.
>
> 특히 주의할 점은 javax 패키지입니다. Spring Boot 3.x부터는 jakarta로 변경되었는데, 저희 환경에서는 반드시 javax를 유지해야 합니다. 초반에 이 부분에서 실수가 많이 발생했고, 결국 프로젝트 전체에 이 규칙을 공유 문서로 못 박아 두었습니다."

---

**[PART 02 — 요구사항 명세]**

> "요구사항 명세로 넘어가겠습니다.
>
> 기능 요구사항을 3개 포털로 분류했습니다. 일반 사용자 포털, 기획사·공연장 담당자를 위한 파트너 포털, 전체 시스템 관리를 위한 어드민 포털입니다.
>
> 이 세 포털은 독립적으로 배포되며, 포트도 8080, 8081, 8082로 분리되어 있습니다.
> 브라우저에서 localhost:8080에 접속하면 사용자 포털, 8082에 접속하면 어드민 포털이 뜨는 방식입니다.
>
> 중요한 점은, 세 포털이 같은 DB를 공유하지만 역할 기반으로 접근이 철저히 제한된다는 것입니다.
> 사용자 계정으로 어드민 포털에 로그인을 시도하면 Spring Security가 차단합니다."

---

**[좌석 예매 상세 플로우 슬라이드]**

> "좌석 예매 플로우를 상세히 보겠습니다.
>
> FR-001부터 FR-006까지 순서대로 읽어보시면, 일반적인 쇼핑몰 주문 플로우와 다른 점이 있습니다.
>
> FR-002에서 Redis가 먼저 좌석을 '임시 선점'합니다. 결제가 완료되지 않아도 일단 다른 사람이 같은 좌석을 선택하지 못하도록 막는 것입니다. 이 선점은 10분 TTL이 있어서, 10분 안에 결제하지 않으면 자동으로 해제됩니다.
>
> 여러분이 실제 티켓 구매할 때 '10분 안에 결제하세요'라는 메시지를 보신 적 있으시죠? 바로 그 원리입니다.
>
> FR-005의 @Scheduled는 3초 간격으로 만료된 선점 좌석을 자동 해제합니다. 이것이 없으면 결제 이탈 사용자의 좌석이 10분 동안 묶여 있게 됩니다."

---

**[PART 03 — 시스템 아키텍처]**

> "시스템 아키텍처입니다.
>
> 이 다이어그램을 보시면 전체 구조가 한눈에 들어옵니다.
>
> 상단 세 박스가 각각 8080, 8081, 8082 포트의 WAR 파일입니다.
> 중앙의 ticket-common은 세 WAR가 공유하는 JAR 파일로, 도메인 클래스, 서비스 메서드, MyBatis 매퍼가 전부 여기에 있습니다.
> 하단 네 박스는 공유 인프라입니다.
>
> 핵심은 세 WAR 간에 직접 의존이 없다는 것입니다.
> ticket-user가 ticket-admin의 클래스를 직접 호출하는 코드가 없습니다.
> 두 포털의 소통은 오직 DB 데이터와 JWT 토큰을 통해서만 이루어집니다.
>
> 이 구조 덕분에 어드민 서버가 다운되어도 사용자 예매 서비스는 계속 동작합니다."

---

---

# 2부 — 라이브 시연 1: 어드민 포털

> **전환 멘트**: "이제 슬라이드에서 설명한 내용을 실제 화면으로 확인하겠습니다. 어드민 포털부터 시작합니다."

---

## 🖥️ DEMO-01: 어드민 로그인 & 대시보드

**[화면: 브라우저 탭1 — localhost:8082/admin/login]**

> "지금 보시는 화면이 어드민 포털 로그인 페이지입니다. 포트 8082입니다.
>
> 어드민 계정으로 로그인하겠습니다."

**→ 입력:**
- 이메일: `admin@ticketlegacy.com`
- 비밀번호: `Cks159753!`
- 로그인 버튼 클릭

> "로그인 시 서버에서 JWT 토큰을 생성하고, 이것을 ADMIN_TOKEN이라는 이름의 HttpOnly 쿠키로 설정합니다.
>
> 개발자도구로 한번 확인해볼게요."

**→ F12 → Application → Cookies → localhost → ADMIN_TOKEN 보여주기**

> "ADMIN_TOKEN 쿠키가 생성된 것이 보이시죠. HttpOnly 체크박스가 활성화되어 있습니다.
> 이 옵션이 켜져 있으면 자바스크립트에서 document.cookie로 이 토큰에 접근할 수 없습니다.
> XSS 공격으로부터 토큰을 보호하는 가장 기본적인 방어입니다."

**→ 개발자도구 닫기 → 대시보드 화면 보여주기**

> "로그인 후 대시보드입니다. 상단에 주요 지표가 표시되고, 하단에 심사 대기 중인 공연, 승인 대기 중인 기획사·공연장 담당자 목록이 보입니다."

---

## 🖥️ DEMO-02: 기획사 승인 (FSM 시연)

**[화면: 대시보드 → 기획사 관리 메뉴]**

> "이번에는 FSM — 유한 상태 기계를 직접 시연해 드리겠습니다.
>
> 저희 데이터베이스에 promoter2@test.com 이라는 기획사 계정이 PENDING 상태로 등록되어 있습니다. 이 계정은 아직 어드민의 승인을 받지 못한 상태입니다."

**→ 어드민 기획사 관리 화면으로 이동**

> "기획사 목록을 보시면 promoter2가 PENDING 상태로 보입니다.
>
> 이 상태에서 가능한 전환은 APPROVED, REJECTED 두 가지뿐입니다.
> SUSPENDED나 다른 상태로 직접 전환하는 것은 FSM이 차단합니다."

**→ promoter2 행의 '승인' 버튼 클릭**

> "승인 처리했습니다. 상태가 PENDING에서 APPROVED로 전환됩니다.
>
> 이제 promoter2는 ticket-partner에 로그인해서 공연을 등록할 수 있습니다."

**→ 같은 행에서 '정지' 버튼 클릭 시도 (가능하면)**

> "APPROVED 상태에서는 SUSPENDED 전환도 가능합니다.
> 만약 코드에서 허용되지 않은 전환을 시도하면, 서버에서 BusinessException이 발생하고 400 오류가 반환됩니다."

---

## 🖥️ DEMO-03: 회원 관리 & 상태 FSM

**[화면: 어드민 → 회원 관리]**

> "회원 관리 화면입니다.
>
> 회원 상태 FSM은 조금 더 복잡합니다. PENDING_APPROVAL, ACTIVE, SUSPENDED, DORMANT, WITHDRAWN 다섯 가지 상태가 있습니다.
>
> 중요한 것은 WITHDRAWN — 탈퇴 상태입니다. 이 상태는 터미널 상태로, 한 번 탈퇴하면 어떤 상태로도 전환이 불가능합니다. 코드 상으로 강제되어 있습니다."

**→ 특정 회원 찾기 → 상태 변경 드롭다운 보여주기**

> "현재 ACTIVE 상태의 회원에게 표시되는 드롭다운을 보시면, SUSPENDED와 WITHDRAWN만 선택 가능합니다.
> DORMANT는 시스템이 자동으로 전환하는 상태라서 어드민이 수동으로 설정할 수 없도록 제거했습니다.
>
> 이것이 FSM의 핵심 가치입니다. 비즈니스 규칙이 코드에 강제되어서, 잘못된 상태 전환이 UI에서조차 노출되지 않습니다."

---

## 🖥️ DEMO-04: 공연 심사 & 승인

**[화면: 어드민 → 대시보드 → 공연 심사 탭]**

> "공연 심사 화면입니다. REVIEW 상태, 즉 심사 대기 중인 공연 목록이 표시됩니다.
>
> 잠시 후 파트너 포털에서 새 공연을 등록한 뒤 다시 이 화면으로 돌아와서 승인 처리를 해보겠습니다.
>
> 지금은 기존에 등록된 공연 하나를 미리보기 해보겠습니다."

**→ 공연 행 클릭 → 미리보기 모달 열기**

> "미리보기 모달에서 공연 상세 정보를 확인할 수 있습니다. 공연 일정, 공연장 정보, 좌석 구역과 가격 등 전체 정보를 검토한 후에 승인 또는 반려를 결정합니다.
>
> 미리보기 없이 바로 승인하는 것은 위험하기 때문에, 11차 세션에서 이 모달을 추가했습니다."

**→ 승인 버튼 클릭**

> "공연 상태가 REVIEW에서 APPROVED로 전환되었습니다.
> APPROVED 공연은 아직 판매 시작 전이고, 게시 버튼을 눌러야 PUBLISHED — 판매 시작 상태가 됩니다."

**→ 게시 버튼 클릭**

> "PUBLISHED 상태가 되었습니다. 이제 ticket-user 포털에서 이 공연을 검색하고 예매할 수 있습니다.
>
> PUBLISHED는 터미널 상태입니다. 한번 게시된 공연은 롤백이 불가능합니다. 실수로 잘못된 공연을 게시하지 않도록 미리보기 단계를 반드시 거치도록 UI를 설계했습니다."

---

---

# 3부 — 라이브 시연 2: 파트너 포털

> **전환 멘트**: "이번에는 파트너 포털입니다. 기획사 담당자 관점에서 공연을 등록하는 과정을 보여드리겠습니다."

---

## 🖥️ DEMO-05: 파트너 로그인

**[화면: 브라우저 탭2 — localhost:8081/partner/login]**

> "포트 8081, 파트너 포털 로그인 페이지입니다.
>
> 주목하실 점은, 파트너 계정은 사용자가 직접 가입할 수 없습니다. 반드시 어드민이 생성하고 승인해야 사용 가능합니다. 이것이 일반 사용자 포털과 다른 점입니다."

**→ 입력:**
- 이메일: `promoter1@test.com`
- 비밀번호: `Cks159753!`
- 로그인 버튼 클릭

> "로그인 성공입니다. PARTNER_TOKEN이라는 별도 쿠키가 생성됩니다.
>
> 파트너 포털은 PROMOTER 역할을 가진 계정만 접근 가능하고, Spring Security가 URL 패턴별로 접근 제어를 합니다. /partner/promoter/** 경로는 PROMOTER 역할이 없으면 403 오류가 발생합니다."

---

## 🖥️ DEMO-06: 공연 등록

**[화면: 파트너 대시보드 → 공연 관리 → 공연 등록]**

> "공연 등록 화면입니다.
>
> 새 공연을 등록해보겠습니다. 이 공연은 DRAFT 상태로 저장됩니다."

**→ 공연 정보 입력:**
- 공연명: `[시연] 스프링 콘서트 2026`
- 카테고리: 콘서트
- 공연장: (드롭다운에서 선택)
- 공연 날짜: (적절한 날짜 선택)
- 설명: `Spring MVC 5.3 기반으로 개발된 공연 예매 시스템 시연용 공연입니다.`
- 등록 버튼 클릭

> "공연이 DRAFT 상태로 등록되었습니다.
>
> DRAFT는 기획사가 작업 중인 상태입니다. 외부에는 공개되지 않고 어드민에도 심사 대기 목록에 뜨지 않습니다.
> 기획사가 모든 정보를 입력하고 '심사 요청'을 눌러야 REVIEW 상태로 전환되면서 어드민의 대시보드에 나타납니다."

---

## 🖥️ DEMO-07: 공연 좌석 등급 설정

**[화면: 파트너 공연 목록 → 등록한 공연 선택 → 좌석 등급 설정]**

> "좌석 등급 설정입니다.
>
> 공연장의 구역별로 좌석 등급과 가격을 설정합니다. VIP석, R석, S석 등으로 나뉘고 구역별로 다른 가격이 적용됩니다."

**→ 좌석 등급 모달 열기 → 구역 선택 → 등급 입력**

> "각 구역에 등급 이름과 가격을 설정했습니다. 이 정보가 사용자 포털에서 좌석 선택 화면에 그대로 표시됩니다.
>
> 이 데이터는 performance_seat_grade 테이블에 저장되고, 예매 시 가격 계산에 사용됩니다."

---

## 🖥️ DEMO-08: 심사 요청 (DRAFT → REVIEW)

**[화면: 공연 상세 → 심사 요청 버튼]**

> "공연 정보를 모두 입력했으니 심사 요청을 하겠습니다."

**→ '심사 요청' 버튼 클릭**

> "DRAFT에서 REVIEW로 상태가 전환되었습니다.
>
> 이제 어드민 대시보드에 이 공연이 '심사 대기' 목록에 나타날 것입니다.
> 잠시 후 어드민 탭으로 돌아가서 이 공연을 승인하는 과정을 보여드리겠습니다.
>
> 현재 이 공연은 파트너에서는 REVIEW로 표시되고, 사용자 포털에서는 여전히 보이지 않습니다."

---

---

# 4부 — 라이브 시연 3: 사용자 포털

> **전환 멘트**: "이제 사용자 포털에서 공연을 예매하는 플로우를 보여드리겠습니다. 앞서 어드민에서 게시(PUBLISHED)한 공연을 예매해보겠습니다."

---

## 🖥️ DEMO-09: 사용자 로그인

**[화면: 브라우저 탭3 — localhost:8080/member/login]**

> "포트 8080, 사용자 포털 로그인 페이지입니다."

**→ 입력:**
- 이메일: `user1@test.com`
- 비밀번호: `Cks159753!`
- 로그인 버튼 클릭

> "USER_TOKEN 쿠키가 생성됩니다. 세 포털이 모두 localhost에서 실행되지만, 쿠키 이름이 다르기 때문에 서로 간섭하지 않습니다.
>
> 만약 USER_TOKEN, PARTNER_TOKEN, ADMIN_TOKEN 세 이름을 모두 ACCESS_TOKEN으로 통일했다면, 어드민 로그인 후 사용자 포털로 이동하면 사용자 쿠키가 덮어씌워지는 버그가 발생합니다. 이 문제를 5차 세션에서 발견하고 분리했습니다."

---

## 🖥️ DEMO-10: 공연 목록 & 검색

**[화면: 메인 페이지 → 공연 목록]**

> "메인 화면입니다. ON_SALE 또는 PUBLISHED 상태의 공연들이 목록으로 표시됩니다.
>
> 데이터가 214개입니다. 이 데이터는 저희가 직접 하나하나 입력한 것이 아닙니다. MySQL의 WITH RECURSIVE CTE — 재귀 공통 테이블 표현식을 사용해서 자동 생성했습니다. 잠시 후 파트 5 슬라이드에서 자세히 설명드리겠습니다."

**→ 공연 하나 클릭**

> "공연 상세 페이지입니다. 포스터 이미지, 공연 정보, 공연회차 목록이 표시됩니다.
>
> 포스터 이미지는 picsum.photos의 시드 URL을 사용했습니다. 공연 ID를 시드로 사용하기 때문에 같은 공연은 항상 같은 이미지가 표시되고, 다른 공연은 다른 이미지가 표시됩니다."

---

## 🖥️ DEMO-11: 좌석 선택 & 예매

**[화면: 공연 상세 → 날짜/회차 선택 → 예매하기 클릭]**

> "예매하기 버튼을 눌렀습니다. 좌석 선택 화면으로 이동합니다."

**→ 좌석 선택 화면에서 구역 선택 → 좌석 클릭**

> "좌석을 선택했습니다. 이 시점에 Redis에 선점 요청이 전송됩니다.
>
> 서버에서는 Lua 스크립트가 실행됩니다. 이 스크립트가 seat_inventory 테이블에서 해당 좌석이 AVAILABLE 상태인지 확인하고, AVAILABLE이면 HOLD로 변경하며, 이미 HOLD나 RESERVED면 실패를 반환합니다.
>
> 이 모든 과정이 원자적으로 실행됩니다. 동시에 100명이 이 버튼을 눌러도, 딱 한 명만 HOLD에 성공합니다."

---

## 🖥️ DEMO-12: 결제 처리

**[화면: 결제 페이지]**

> "결제 페이지입니다. 선택한 좌석 정보와 가격이 표시됩니다.
>
> 현재 결제 게이트웨이는 시뮬레이션 모드입니다. 실제 PG사 연동은 Phase 3에서 진행할 예정입니다. 지금은 결제 버튼을 누르면 항상 성공하는 mock 상태입니다."

**→ 결제 버튼 클릭**

> "결제 완료입니다.
>
> 서버에서는 다음이 발생했습니다.
> 첫째, payment 테이블에 idempotency_key와 함께 결제 레코드가 생성됩니다.
> 둘째, reservation 상태가 PENDING에서 CONFIRMED로 전환됩니다.
> 셋째, seat_inventory 상태가 HOLD에서 RESERVED로 전환됩니다.
>
> 이 세 가지 변경이 하나의 트랜잭션으로 처리됩니다. 도중에 오류가 발생하면 전체가 롤백됩니다."

---

## 🖥️ DEMO-13: 예매 내역 확인

**[화면: 마이페이지 → 예매 내역]**

> "마이페이지에서 방금 예매한 내역을 확인할 수 있습니다. CONFIRMED 상태로 표시됩니다."

---

## 🖥️ DEMO-14: 예매 취소

**[화면: 예매 내역 → 취소 버튼]**

> "예매를 취소해보겠습니다."

**→ 취소 버튼 클릭 → 확인 다이얼로그 확인**

> "취소 완료입니다. 상태가 CONFIRMED에서 CANCELLED로 전환되었고, seat_inventory도 RESERVED에서 AVAILABLE로 복원됩니다.
>
> 이제 다른 사용자가 이 좌석을 다시 예매할 수 있습니다.
>
> 환불 상태(REFUNDED)는 별도로 관리됩니다. CANCELLED는 단순 취소이고, 실제 환불이 처리되면 REFUNDED로 추가 전환됩니다. 이 부분은 Phase 4에서 완성할 예정입니다."

---

---

# 5부 — 슬라이드 발표 (파트 4~5)

> **전환 멘트**: "라이브 시연에서 보신 기능들의 기술적 구현 방식을 파트 4에서 자세히 설명드리겠습니다."

---

**[Dual Defense 슬라이드]**

> "좌석 예매 동시성 제어 — Dual Defense입니다.
>
> 1단계는 Redis Lua 스크립트입니다. Redis는 기본적으로 싱글 스레드로 명령을 처리합니다. 그리고 Lua 스크립트는 Redis에서 하나의 원자적 명령으로 실행됩니다. 이 말은, Lua 스크립트 실행 중에 다른 클라이언트의 요청이 끼어들 수 없다는 뜻입니다.
>
> 스크립트 로직은 간단합니다. 좌석 키의 현재 값이 AVAILABLE이면 HOLD로 변경하고 성공을 반환. AVAILABLE이 아니면 변경 없이 실패를 반환. 이 두 단계가 원자적으로 실행됩니다.
>
> 2단계는 DB의 조건부 UPDATE입니다. Redis가 장애 상태여도 WHERE status='AVAILABLE' 조건이 두 번째 방어선이 됩니다. Redis 선점 없이 DB에 직접 UPDATE를 시도해도, 두 번째 시도는 WHERE 조건을 만족하지 못해 0 rows affected가 반환됩니다.
>
> 3단계의 idempotency_key는 클라이언트가 네트워크 오류로 같은 결제를 두 번 요청할 때 두 번째 요청을 무시하는 메커니즘입니다. UNIQUE 제약이 있기 때문에 중복 INSERT 시 DB에서 오류가 발생하고, 서버가 이를 잡아서 '이미 처리된 결제'로 응답합니다."

---

**[FSM 슬라이드]**

> "FSM 상태 전환 다이어그램입니다.
>
> 4개 도메인에 FSM을 적용했습니다. 각 Enum의 canTransitionTo() 메서드가 핵심입니다.
>
> 예를 들어 MemberStatus.WITHDRAWN.canTransitionTo(ACTIVE)를 호출하면 false가 반환됩니다. 서비스 코드에서는 이 반환값이 false이면 BusinessException을 던집니다. 이 예외는 GlobalExceptionHandler가 잡아서 400 오류로 응답합니다.
>
> 이 구조의 장점은 상태 전환 규칙이 분산되지 않는다는 것입니다. 어디서 상태를 바꾸든, 같은 Enum의 같은 메서드를 통과해야 합니다. 규칙을 바꾸고 싶으면 Enum 하나만 수정하면 됩니다."

---

**[인증 & 보안 슬라이드]**

> "인증 아키텍처입니다.
>
> 사용자가 로그인하면 서버에서 JWT를 생성합니다. 이 JWT에는 사용자 ID와 역할(Role)이 담겨 있습니다.
>
> 이 JWT를 HttpOnly 쿠키로 설정해서 클라이언트에 전달합니다. 이후 모든 요청에서 브라우저가 자동으로 이 쿠키를 포함해서 전송합니다.
>
> 서버의 JwtAuthenticationFilter가 쿠키에서 JWT를 꺼내 서명을 검증하고, SecurityContext에 인증 정보를 설정합니다. 이후 Spring Security가 URL 패턴별로 역할을 체크합니다.
>
> 세 서버가 동일한 JWT_SECRET_KEY 환경변수를 공유하기 때문에, ticket-user에서 발급한 JWT를 ticket-admin이 검증할 수는 있습니다. 하지만 어드민 포털의 security-context.xml에서 SUPER_ADMIN 역할이 없으면 접근을 차단하기 때문에, 일반 사용자가 어드민 포털을 사용하는 것은 불가능합니다."

---

**[데이터셋 슬라이드 — 차트]**

> "데이터셋 구성 현황입니다.
>
> 초기에 공연 데이터가 14개밖에 없었습니다. Elasticsearch 연동 테스트나 성능 검증을 하기에는 턱없이 부족한 양입니다.
>
> 그래서 WITH RECURSIVE CTE를 활용한 자동 생성 스크립트를 작성했습니다. 공연을 생성하면 연결된 공연회차, 좌석 등급, 좌석, 좌석 인벤토리가 계단식으로 생성됩니다.
>
> 차트를 보시면 공연 214건에 대해 좌석 인벤토리가 12,840개 이상 생성된 것이 확인됩니다. 수동으로 입력했다면 수 시간이 걸렸을 작업을 SQL 스크립트 한 번으로 해결했습니다.
>
> 또한 poster_url은 picsum.photos 시드 URL로 정규화했고, description은 HTML 형식으로 저장되어 향후 Elasticsearch 인덱싱 준비가 완료된 상태입니다."

---

---

# 6부 — 라이브 시연 4: 기술 심화 시연

> **전환 멘트**: "이번에는 코드 레벨의 기술적인 부분을 직접 보여드리겠습니다. API 오류 처리와 FSM 위반을 직접 시연해드리겠습니다."

---

## 🖥️ DEMO-15: FSM 위반 시 오류 응답 확인

**[화면: 어드민 탭 → 개발자도구 Console 열기]**

> "FSM 위반을 직접 API 호출로 테스트해보겠습니다.
>
> PUBLISHED 상태의 공연을 다시 REVIEW로 되돌리려고 시도해보겠습니다. PUBLISHED는 터미널 상태이기 때문에 이 전환은 불가능합니다."

**→ Console에서 fetch 실행 또는 별도 Postman/HTTP 클라이언트 사용**

```javascript
// 브라우저 Console에 입력 (어드민 로그인 상태에서)
fetch('/backoffice/super/api/performances/1/rollback-to-draft', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' }
}).then(r => r.json()).then(console.log)
```

> "응답이 왔습니다. status 400, errorCode PERFORMANCE_STATUS_INVALID_TRANSITION, message '허용되지 않는 상태 전환입니다' — 예상대로 오류가 반환됩니다.
>
> 이 오류는 PerformanceApprovalStatus Enum의 validateTransition()이 던진 BusinessException을 GlobalExceptionHandler가 잡아서 ApiResponse 형식으로 반환한 것입니다.
>
> 클라이언트는 success: false와 errorCode만 확인하면 어떤 오류인지 파악할 수 있습니다."

---

## 🖥️ DEMO-16: 코드 구조 간략 설명

**[화면: VS Code 또는 STS에서 코드 열기]**

> "코드를 잠깐 보여드리겠습니다."

**→ `MemberStatus.java` 열기**

> "MemberStatus Enum입니다. canTransitionTo() 메서드가 switch문으로 각 상태에서 허용되는 전환 목록을 반환합니다. 이 메서드 하나가 전체 FSM 규칙의 단일 진실의 원천입니다."

**→ `MemberService.java` 열기 → updateAdminStatus() 메서드**

> "서비스 코드에서는 단 두 줄입니다. canTransitionTo() 검증하고, 통과하면 상태 업데이트. 비즈니스 로직이 이처럼 간결하게 유지됩니다."

**→ `GlobalExceptionHandler.java` 열기 (ticket-admin/ticket-common)**

> "GlobalExceptionHandler에서 BusinessException을 잡아 ApiResponse 형식으로 변환합니다. 세 WAR가 동일한 핸들러를 공유하므로, 에러 응답 형식이 항상 일관됩니다."

---

---

# 7부 — 슬라이드 발표 (파트 6~7)

**[기술적 도전 슬라이드]**

> "개발 과정에서 만난 핵심 이슈 4가지입니다.
>
> 첫 번째, JSP EL vs JS 템플릿 리터럴 충돌입니다.
> 처음에 JavaScript에서 ${item.name} 이런 형태로 DOM을 조작하는 코드를 작성했습니다.
> 그런데 실행해보니 item.name이 항상 false로 출력되는 현상이 발생했습니다.
> 원인을 찾아보니, JSP 엔진이 서버에서 HTML을 생성할 때 ${}를 먼저 평가해버렸습니다. item이라는 JSP 변수가 없으니 false로 평가된 것입니다.
> 해결 방법은 간단합니다. JS 템플릿 리터럴 대신 문자열 연결을 사용하면 됩니다. item.name 부분을 '' + item.name + '' 형태로 전면 교체했습니다.
>
> 두 번째, BCrypt 해시 불일치입니다.
> 세션을 거듭하면서 SQL 파일이 3개로 분산됐고, 각 파일에서 서로 다른 BCrypt 해시를 사용했습니다. 결과적으로 모든 계정의 로그인이 불가능해졌습니다.
> 12차 세션에서 schema_total.sql, data_total.sql로 단일화하고 해시를 전부 교체해서 해결했습니다.
>
> 세 번째는 Redis 장애 시 ticket-user 기동 실패입니다.
> LettuceConnectionFactory가 Redis 연결에 실패하면 즉시 예외를 던지고 서버 기동을 중단합니다.
> 이를 graceful 강등으로 해결했습니다. Redis가 없으면 좌석 선점 단계를 건너뛰고 DB만으로 처리하는 폴백 로직을 추가했습니다.
>
> 네 번째는 Mapper 직접 주입 문제입니다. 초기 코드에서 컨트롤러가 Mapper를 9개나 직접 주입하고 있었습니다. 트랜잭션 경계가 없고, 코드 재사용이 불가능한 상태였습니다. 8차부터 11차 세션에 걸쳐 단계적으로 서비스 계층으로 이전했습니다."

---

**[향후 계획 슬라이드]**

> "향후 계획입니다.
>
> Phase 3의 최우선 작업은 결제 PG 실제 연동입니다. 현재 PaymentService.simulatePgPayment()가 항상 true를 반환하는 mock 상태인데, 이것은 실제 운영에서 절대 사용할 수 없는 코드입니다.
>
> Phase 4에서 가장 기대되는 것은 Elasticsearch 연동입니다. 현재 DB에 214개의 공연 데이터가 있고, description과 poster_url이 이미 Elasticsearch 친화적인 형식으로 저장되어 있습니다. full-text 검색과 카테고리·가격 필터를 추가하면 사용자 경험이 크게 개선될 것입니다.
>
> Phase 5는 코드 정리입니다. DateUtil이 ticket-common과 ticket-user 양쪽에 중복으로 존재하는데, 이런 코드 중복은 유지보수 시 한쪽만 수정하는 실수를 유발합니다. 이 정리가 기술적으로 어렵지는 않지만, 반드시 해야 하는 작업입니다."

---

**[결론 슬라이드]**

> "결론입니다.
>
> TicketLegacy는 Spring Legacy MVC 5.3 기반 엔터프라이즈 수준의 공연 예매 플랫폼입니다.
>
> 45개 테이블, 214개 공연 데이터, 3개 독립 WAR 포털, 4개 도메인 FSM — 이 숫자들이 이 프로젝트의 완성도를 보여줍니다.
>
> 하지만 저희 팀이 이 프로젝트에서 얻은 가장 큰 것은 숫자가 아닙니다.
>
> '동작하면 끝'이 아니라 '운영에서 안 터지는가'를 질문하는 사고방식. 트랜잭션 경계, 동시성, 멱등성, 보안 — 이 네 가지를 모든 코드 변경에서 자동으로 검토하는 습관. 레거시 시스템의 설정 파일을 두려워하지 않고 직접 다룰 수 있는 역량.
>
> 이것이 저희가 TicketLegacy를 통해 얻은 진짜 성과입니다.
>
> 발표 들어주셔서 감사합니다."

---

---

# 8부 — Q&A 예상 질문 & 답변

> 아래 질문들은 발표 후 나올 가능성이 높은 것들입니다. 미리 숙지해두세요.

---

### Q1. Redis 없이도 서비스가 되나요? Redis가 다운되면 어떻게 되나요?

> "네, 동작합니다. Redis 장애 시 DB-only 모드로 자동 강등됩니다.
>
> 구체적으로는 Redis 선점 요청이 실패하면 try-catch에서 잡아서, Redis 없이 바로 DB UPDATE로 진행합니다. DB의 WHERE status='AVAILABLE' 조건이 두 번째 방어선이 됩니다.
>
> 성능은 저하됩니다. Redis가 있을 때는 DB hit 없이 Redis에서 먼저 필터링이 되지만, Redis 없이는 모든 선점 시도가 DB 쿼리로 이어집니다. 하지만 서비스 자체는 중단되지 않습니다."

---

### Q2. 세 서버가 같은 JWT_SECRET_KEY를 쓰면 보안 문제가 없나요? 어드민 토큰으로 유저 포털에 접근하면?

> "접근 자체는 물리적으로 가능합니다. 같은 시크릿으로 서명됐으니까요.
>
> 하지만 ticket-user의 security-context.xml에서 역할 기반으로 접근을 제어합니다. 어드민 계정의 JWT에는 SUPER_ADMIN 역할이 담겨 있고, 사용자 포털은 MEMBER 역할을 요구하기 때문에 접근이 차단됩니다.
>
> 정확히는, 같은 시크릿을 쓰는 것은 '토큰 위조 방지'이고, 역할 기반 접근 제어는 별개입니다. 두 레이어가 각자 역할을 수행합니다.
>
> 실제 운영 환경이라면 포털별로 별도 시크릿을 쓰는 것이 더 안전합니다. 이것은 개선 포인트로 인식하고 있습니다."

---

### Q3. 동시에 두 사람이 정말 같은 좌석을 선택하면 어떻게 되나요? 테스트해봤나요?

> "네, 테스트해봤습니다. JMeter로 동일 좌석에 동시 요청을 보냈을 때 한 명만 성공하는 것을 확인했습니다.
>
> Lua 스크립트가 원자적으로 실행되기 때문에, 두 요청이 동시에 들어와도 Redis 서버에서 순차적으로 처리됩니다. 첫 번째 요청이 AVAILABLE → HOLD로 바꾸면, 두 번째 요청이 실행될 때 이미 HOLD 상태이므로 실패를 반환합니다.
>
> 응답을 받은 두 번째 사용자는 '이미 선점된 좌석입니다' 메시지를 보게 됩니다."

---

### Q4. Spring Boot를 쓰지 않은 이유가 있나요? 오히려 불편하지 않았나요?

> "의도적인 선택이었습니다. 불편했습니다. (웃음)
>
> 하지만 그 불편함이 학습의 가치였습니다. Spring Boot는 많은 것을 자동으로 해줍니다. 그 편리함 뒤에서 무슨 일이 일어나는지 모르고 사용하면, 문제가 생겼을 때 원인을 찾기 어렵습니다.
>
> 직접 root-context.xml에 Bean을 등록하고, security-context.xml에 필터 체인을 구성하면서, Spring이 내부적으로 어떻게 동작하는지 이해할 수 있었습니다.
>
> 또한 실제 현업에서는 아직도 Spring Legacy 기반의 시스템이 많이 운영됩니다. 이런 시스템을 유지보수하거나 점진적으로 마이그레이션하는 역량이 실제로 필요합니다."

---

### Q5. ticket-common의 서비스 메서드를 바꾸면 3개 WAR를 전부 수정해야 하지 않나요? 관리가 어렵지 않나요?

> "맞습니다. 그것이 이 아키텍처의 트레이드오프입니다.
>
> 저희 프로젝트 문서에 이 규칙을 명시해뒀습니다. ticket-common의 서비스 메서드를 수정하기 전에 반드시 3개 WAR 전체에서 해당 메서드를 grep으로 검색해서 모든 호출처를 확인해야 한다고요.
>
> 실제로 이 규칙을 어겨서 컴파일 에러가 발생한 적이 있고, 그 이후로는 항상 검색을 먼저 합니다.
>
> 이 아키텍처의 장점은 비즈니스 로직이 한 곳에 집중된다는 것입니다. 코드 중복이 없고, 상태 전환 로직이 항상 일관됩니다."

---

### Q6. MyBatis를 선택한 이유가 있나요? JPA를 쓰지 않은 이유는?

> "두 가지 이유입니다.
>
> 첫째, 복잡한 쿼리를 직접 제어하고 싶었습니다. 좌석 선점처럼 WHERE status='AVAILABLE'이라는 조건부 UPDATE가 핵심인 경우, JPA의 더티체킹으로는 의도가 명확하게 드러나지 않습니다. MyBatis의 XML 쿼리는 실행될 SQL이 그대로 보입니다.
>
> 둘째, 현업에서 MyBatis 레거시 시스템이 많습니다. 이 환경을 경험해두는 것이 취업 후에도 도움이 됩니다.
>
> 단점은 resultMap을 직접 관리해야 하는 번거로움입니다. 도메인 필드와 컬럼명이 다를 때 resultMap을 일일이 맞춰줘야 합니다. 실제로 Payment 도메인에서 이 불일치 문제가 발생했었습니다."

---

### Q7. 회원가입은 어떻게 하나요? BCrypt는 직접 구현했나요?

> "회원가입은 POST /api/member/join으로 처리합니다. Spring Security의 BCryptPasswordEncoder를 사용하고, 직접 구현하지 않았습니다.
>
> BCrypt는 단방향 해시입니다. 원문 비밀번호를 저장하지 않고, 로그인 시 입력받은 비밀번호와 DB의 해시값을 matches()로 비교합니다. 해시가 일치하면 로그인 성공입니다.
>
> 초기에 SQL 시드 파일에서 BCrypt 해시 버전이 달라서 모든 계정 로그인이 안 되는 문제가 있었습니다. $2a$10$ 버전으로 통일해서 해결했습니다."

---

### Q8. Redis 좌석 선점 시간이 10분인 이유가 있나요? 더 짧거나 길면 어떻게 되나요?

> "10분은 실제 티켓 서비스에서 일반적으로 사용하는 시간입니다. 인터파크도 비슷한 시간을 씁니다.
>
> 너무 짧으면 (예: 1분) 사용자가 결제 정보를 입력하다가 시간이 만료되어 다시 좌석 선택부터 해야 하는 불편함이 생깁니다.
>
> 너무 길면 (예: 1시간) 결제를 포기하고 이탈한 사용자의 좌석이 오래 묶여서 다른 사람이 예매할 기회를 잃게 됩니다.
>
> 10분은 결제를 완료하기에 충분하면서, 이탈 시 좌석이 너무 오래 묶이지 않는 균형점입니다."

---

### Q9. 현재 미완성이거나 mock인 부분이 있나요?

> "솔직하게 말씀드리겠습니다. 두 가지 있습니다.
>
> 첫째, 결제 게이트웨이가 mock입니다. PaymentService.simulatePgPayment()가 항상 true를 반환합니다. Phase 3에서 실제 PG 연동 예정입니다.
>
> 둘째, 환불 플로우가 미완성입니다. CANCELLED 상태 전환은 구현되어 있지만, 실제 결제 취소 요청을 PG에 보내고 REFUNDED 상태로 전환하는 부분이 없습니다.
>
> 이 두 가지는 의도적으로 남겨두었습니다. 실제 PG 연동에는 계약이 필요하고, 학습 프로젝트에서는 구조를 먼저 잡는 것이 더 중요하다고 판단했습니다."

---

### Q10. 3개 서버를 동시에 운영하면 서버 리소스가 3배 아닌가요?

> "맞습니다. 각 WAR가 독립 JVM 프로세스에서 실행되므로 메모리 사용량이 3배입니다.
>
> 개발 환경에서는 각 WAR가 약 300~400MB RAM을 사용하므로 총 1GB 이상이 필요합니다.
>
> 이 아키텍처는 마이크로서비스와 모놀리스의 중간 형태입니다. 완전한 마이크로서비스보다 구성이 단순하지만, 단일 WAR보다 역할 분리가 명확합니다.
>
> 실제 운영에서는 사용자 트래픽이 가장 많은 ticket-user만 스케일아웃하고, 어드민은 소규모로 운영하는 방식으로 비용 최적화가 가능합니다."

---

---

## 🚨 트러블슈팅 — 시연 중 문제 발생 시

| 상황 | 원인 | 즉시 조치 |
|------|------|-----------|
| 로그인이 안 됨 | 쿠키 충돌 또는 서버 미기동 | F12 → Cookies에서 해당 토큰 삭제 후 재로그인 |
| 404 오류 | 해당 서버가 기동되지 않음 | 해당 포트 터미널 확인, 재기동 |
| 500 오류 | DB 연결 실패 또는 NPE | MySQL 기동 확인, 로그 확인 |
| 좌석 선택 안 됨 | Redis 미기동 | redis-cli ping 확인 후 재기동 |
| 공연 목록 0건 | DB 시드 미적용 | mysql < db/data_total.sql 실행 |
| 화면이 안 바뀜 | JS 오류 | F12 Console 에러 확인 |

---

## 📝 마지막 체크

```
발표 종료 후:
□ 시연 중 등록한 '[시연] 스프링 콘서트 2026' 공연 삭제 (선택)
□ 3개 서버 종료 (Ctrl+C)
□ 발표 자료 제출 전 ticketlegacy_presentation.pptx 확인
```

---

> **이 파일은 발표 당일 이 탭을 열어두고, 발표자가 화면을 시연하는 동안 옆에서 대사를 읽어주는 방식으로 사용하세요.**
