# TicketLegacy 라이브 시연 대본 (상세판)
> **보조 모니터 전용** — 발표자가 화면을 보며 진행하는 동안 옆에서 이 파일을 보고 대사를 읽는다  
> `→ 행동` = 지금 클릭하거나 입력할 것  
> 일반 텍스트 = 그대로 말할 대사  
> `// 주석` = 진행자 내부 메모 (말하지 않음)

---

## 시연 시작 전 30분 — 필수 준비

```bash
# 터미널 1 — 사용자 서버
mvn tomcat7:run -pl ticket-user -am
# "INFO  ContextLoader - Root WebApplicationContext initialized" 로그 나오면 준비 완료

# 터미널 2 — 파트너 서버
mvn tomcat7:run -pl ticket-partner -am

# 터미널 3 — 어드민 서버
mvn tomcat7:run -pl ticket-admin -am

# 터미널 4 — Redis 확인
redis-cli ping
# PONG 나와야 함. 안 나오면 redis-server 실행

# DB 확인
mysql -uroot -p1234 springgreen6 -e "SELECT COUNT(*) FROM performance"
# 200 이상 나와야 함
```

```
브라우저 탭 준비
  탭 A: localhost:8082/admin/login    ← 어드민
  탭 B: localhost:8081/partner/login  ← 파트너
  탭 C: localhost:8080/member/login   ← 사용자

쿠키 초기화
  F12 → Application → Cookies → localhost
  USER_TOKEN, PARTNER_TOKEN, ADMIN_TOKEN 전부 삭제

계정 메모 (옆에 보이도록)
  admin@ticketlegacy.com  /  Cks159753!
  promoter1@test.com      /  Cks159753!
  promoter2@test.com      /  Cks159753!   ← PENDING 상태, 승인 시연용
  user1@test.com          /  Cks159753!
```

---
---

# 시연 1 — 어드민 포털
## 목표: 로그인 → JWT 쿠키 확인 → 기획사 FSM 승인 → 회원 상태 FSM → 공연 심사·게시

---

### A-1. 어드민 포털 로그인

→ **탭 A 클릭 (localhost:8082/admin/login)**

"지금 열린 화면이 어드민 포털 로그인 페이지입니다. 주소창 보시면 포트가 8082번입니다.

저희 프로젝트는 세 개의 포털이 각각 독립된 서버로 동작합니다. 8080은 일반 사용자, 8081은 파트너, 8082는 어드민. 포트만 달라도 완전히 다른 애플리케이션입니다. 같은 DB를 쓰지만 접근할 수 있는 기능과 역할이 완전히 분리되어 있습니다.

이 어드민 포털은 일반 사용자가 아무리 URL을 알고 들어와도 로그인이 안 됩니다. Spring Security에서 SUPER_ADMIN이나 STAFF 역할을 가진 계정만 통과시키도록 막아놨기 때문입니다."

→ **이메일 입력창 클릭 → `admin@ticketlegacy.com` 입력**

→ **비밀번호 입력창 클릭 → `Cks159753!` 입력**

"비밀번호는 BCrypt로 해싱되어 DB에 저장됩니다. BCrypt는 단방향 해시라서 원문 비밀번호가 DB에 절대 저장되지 않습니다. 로그인할 때는 입력한 비밀번호를 다시 해싱해서 DB에 저장된 해시값과 비교하는 방식으로 검증합니다.

한 가지 재미있는 사실을 말씀드리면, BCrypt는 같은 비밀번호로 해싱해도 매번 다른 결과가 나옵니다. 솔트값을 랜덤으로 섞기 때문입니다. 그래서 DB에서 admin 계정과 user1 계정의 해시값을 비교해도 비밀번호가 같은지 알 수 없습니다."

→ **로그인 버튼 클릭**

"로그인이 됐습니다. 지금 이 순간 서버 내부에서 뭔가 일어났는데, 그걸 바로 눈으로 보여드리겠습니다."

→ **F12 누르기 → 상단 탭에서 Application 클릭 → 왼쪽 사이드바 Cookies 펼치기 → localhost 클릭**

"여기를 보시면 ADMIN_TOKEN이라는 쿠키가 생성된 것이 보입니다. 이게 JWT입니다.

오른쪽 속성을 보시면 HttpOnly라는 열이 있는데 체크가 되어 있습니다. 이게 왜 중요한지 직접 보여드리겠습니다."

→ **상단 탭에서 Console 클릭 → `document.cookie` 입력 → 엔터**

"실행해보면 ADMIN_TOKEN이 보이지 않습니다. 일반 쿠키들은 여기에 뜨는데 HttpOnly 쿠키는 자바스크립트가 접근 자체를 못 합니다.

이게 왜 중요하냐면, XSS 공격을 생각해보시면 됩니다. 누군가 댓글 같은 곳에 악성 자바스크립트를 심어두면, 그 페이지를 방문한 사용자의 브라우저에서 그 스크립트가 실행됩니다. 만약 토큰이 일반 쿠키나 localStorage에 있다면 document.cookie나 localStorage.getItem으로 토큰을 빼낼 수 있습니다. 하지만 HttpOnly 쿠키는 자바스크립트로 접근 자체가 불가능하기 때문에 XSS로 토큰을 훔치는 게 원천 차단됩니다."

→ **F12 닫기**

"그리고 이 쿠키 이름이 ADMIN_TOKEN이라는 점도 중요합니다. 사용자 포털은 USER_TOKEN, 파트너 포털은 PARTNER_TOKEN이라는 별도 이름을 씁니다.

왜 이렇게 분리했냐면, 개발 초기에 세 포털이 전부 ACCESS_TOKEN이라는 같은 이름을 쓰고 있었습니다. 그랬더니 localhost에서 어드민에 로그인한 뒤 사용자 포털 탭으로 이동하면, 사용자 포털이 로그아웃 처리가 돼버리는 버그가 생겼습니다. 어드민 쿠키가 사용자 쿠키를 덮어씌운 겁니다. 5차 세션에서 이 문제를 발견하고 이름을 전부 분리해서 해결했습니다."

---

### A-2. 대시보드 전체 훑어보기

"로그인하면 이 대시보드가 첫 화면입니다.

상단에 주요 지표들이 보입니다. 전체 회원 수, 이번 달 예매 건수, 심사 대기 중인 공연 수, 승인 대기 중인 기획사 수 같은 것들입니다."

→ **대시보드 상단 카드 영역 가리키기**

"이 숫자들이 어떻게 채워지냐면, 페이지가 로딩되면 JavaScript가 백그라운드에서 API 6개를 동시에 fetch합니다. 응답이 오는 대로 해당 영역에 데이터를 채웁니다. 서버 렌더링이 아닌 클라이언트 사이드에서 Ajax로 처리하는 방식입니다.

재미있는 사실은, 이 대시보드 API가 처음에는 하나도 없었습니다. JSP가 API를 호출하는데 서버에 엔드포인트가 없으니까 화면이 텅 비어 있었고, 숫자가 다 0이거나 에러가 났습니다. 2차 세션에서 API를 전부 구현하고 그제야 제대로 동작했습니다."

→ **페이지 아래로 천천히 스크롤**

"아래로 내려오면 심사 대기 중인 공연 목록, 최근 승인 요청한 기획사 목록, 최근 예매 내역이 보입니다. 어드민이 한 화면에서 전체 상황을 파악할 수 있도록 구성했습니다."

---

### A-3. 기획사 FSM 승인 시연

→ **왼쪽 사이드 메뉴에서 기획사 관리 클릭**

"기획사 관리 페이지입니다. 여기서 FSM을 직접 보여드리겠습니다.

FSM이 뭔지 먼저 한 문장으로 정의하면, '허용된 상태 전환만 코드 레벨에서 강제하는 구조'입니다. 도메인 규칙을 Enum 안에 하드코딩해서, 잘못된 전환이 어디서 시도되든 항상 거절되도록 만든 겁니다."

→ **목록에서 promoter2@test.com 행 찾기 — PENDING 상태 확인**

"promoter2@test.com이 PENDING 상태입니다. PENDING은 '어드민의 승인을 기다리는 상태'입니다. 이 계정으로는 지금 파트너 포털에 로그인이 불가능합니다.

이 PENDING 상태에서 갈 수 있는 상태는 두 가지뿐입니다. APPROVED — 승인, 또는 REJECTED — 반려. 이 두 가지 외의 전환은 FSM이 막습니다."

→ **행 끝에 있는 버튼들 확인 — 승인, 반려 버튼만 있어야 함**

"버튼을 보시면 승인과 반려만 있습니다. SUSPENDED 버튼이 없죠? 아직 승인도 안 된 기획사를 정지시키는 건 말이 안 됩니다. 비즈니스 로직상 PENDING에서 SUSPENDED 전환은 없기 때문에 버튼 자체를 렌더링하지 않습니다.

이것이 FSM이 UI에도 영향을 미치는 방식입니다. 서버 로직뿐만 아니라 화면 구성까지 FSM 규칙을 따릅니다."

→ **승인 버튼 클릭**

"승인 처리했습니다."

→ **상태 열을 확인 — PENDING에서 APPROVED로 변경됨**

"상태가 PENDING에서 APPROVED로 바뀌었습니다. 이제 이 계정은 파트너 포털 8081에 로그인할 수 있습니다.

내부적으로 어떤 일이 일어났는지 설명드리면, 서비스 메서드에서 먼저 PromoterApprovalStatus.PENDING.canTransitionTo(APPROVED)를 호출합니다. 이게 true를 반환하면 DB를 UPDATE합니다. false를 반환하면 BusinessException이 발생합니다. 이번엔 PENDING에서 APPROVED는 허용된 전환이라 성공한 겁니다."

→ **APPROVED로 바뀐 promoter2 행 보여주기 — 이제 버튼이 다르게 표시됨**

"이제 APPROVED 상태가 됐으니 버튼 구성이 달라졌습니다. SUSPENDED와 REJECTED 버튼이 있습니다. PENDING으로 되돌리는 버튼은 없습니다. APPROVED에서 PENDING으로 가는 것은 FSM이 허용하지 않습니다.

이걸 API 레벨에서도 직접 보여드리겠습니다."

→ **F12 → Console 탭 클릭 → 아래 코드 입력 후 엔터**

```javascript
// APPROVED 상태인 기획사를 PENDING으로 되돌리는 잘못된 요청
fetch('/backoffice/super/api/promoters/2/status', {
  method: 'PUT',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({status: 'PENDING'})
}).then(r => r.json()).then(d => console.log(JSON.stringify(d, null, 2)))
```

"API를 직접 호출했습니다. 응답을 보시면,"

→ **Console 응답 결과 천천히 읽기**

"success가 false입니다. errorCode는 PRO_005입니다. message는 '허용되지 않는 상태 전환입니다.' 400 오류가 반환됐습니다.

이 응답이 만들어지는 과정을 설명드리면, PromoterApprovalStatus.APPROVED.canTransitionTo(PENDING)이 false를 반환합니다. 서비스 코드가 BusinessException을 던집니다. ticket-admin에 있는 GlobalExceptionHandler가 이 예외를 잡아서 ApiResponse 형식으로 변환해서 반환합니다. 에러 코드 PRO_005는 ErrorCode Enum에 PROMOTER_STATUS_INVALID_TRANSITION으로 정의되어 있고, PRO는 Promoter 도메인, 005번 오류라는 의미입니다."

→ **F12 닫기**

---

### A-4. 회원 관리 FSM 상세 시연

→ **왼쪽 메뉴에서 회원 관리 클릭**

"회원 관리 화면입니다. 기획사 FSM보다 회원 FSM이 조금 더 복잡합니다. 상태가 다섯 가지입니다.

PENDING_APPROVAL — 가입 후 승인 대기. ACTIVE — 정상 활성 상태. SUSPENDED — 관리자가 정지시킨 상태. DORMANT — 장기 미접속으로 시스템이 자동 전환. WITHDRAWN — 탈퇴.

이 다섯 가지 사이에 모든 전환이 가능한 게 아닙니다. FSM이 규정한 경로만 이동할 수 있습니다."

→ **목록에서 ACTIVE 상태인 회원 한 명 찾기 → 상태 변경 버튼 또는 드롭다운 찾기**

"ACTIVE 상태 회원의 변경 버튼을 보시면 SUSPENDED와 WITHDRAWN만 있습니다. DORMANT가 없습니다.

DORMANT는 '오랫동안 접속 안 한 사용자를 시스템이 자동으로 전환하는 상태'입니다. 관리자가 수동으로 특정 사람을 DORMANT로 만드는 건 말이 안 됩니다. 비즈니스 규칙상 DORMANT는 시스템 전용 전환이라서 UI에서 아예 제거했습니다."

→ **SUSPENDED 선택 → 확인 버튼 클릭**

"SUSPENDED로 변경됐습니다. 이 상태가 되면 이 회원이 로그인하려고 하면 '계정이 정지된 상태입니다'라는 메시지를 받게 됩니다. 정지된 회원은 예매도, 취소도, 어떤 기능도 사용 불가능합니다."

→ **같은 회원의 드롭다운 다시 열기**

"SUSPENDED 상태가 되니까 선택지가 달라졌습니다. ACTIVE로 복귀하거나 WITHDRAWN으로 탈퇴 처리가 가능합니다. PENDING_APPROVAL로 돌아가는 건 없습니다. 이미 가입한 사람을 다시 가입 대기 상태로 만드는 건 의미가 없으니까요."

→ **ACTIVE 선택 → 확인**

"다시 ACTIVE로 복귀됐습니다. 이제 WITHDRAWN을 해보겠습니다. WITHDRAWN은 터미널 상태입니다. 한번 탈퇴하면 어떤 상태로도 돌아올 수 없습니다."

→ **다른 회원(테스트용, user3 같은 계정) 찾기 → WITHDRAWN 선택**

"SweetAlert 확인 다이얼로그가 떴습니다. '탈퇴 처리는 되돌릴 수 없습니다. 정말 처리하시겠습니까?' 터미널 상태이기 때문에 한 번 더 확인을 받습니다. 실수로 탈퇴 처리하는 것을 막기 위한 UX 처리입니다."

→ **확인 클릭**

"WITHDRAWN으로 변경됐습니다."

→ **해당 회원 행 다시 보여주기 — 상태 변경 버튼 없음 확인**

"이 회원 행을 보시면 상태 변경 버튼이 없습니다. 아무것도 없습니다. 코드에서 WITHDRAWN 상태인 경우 드롭다운 자체를 렌더링하지 않도록 처리했습니다. '변경 불가' 텍스트만 표시됩니다.

이게 FSM의 강력함입니다. 서버 로직에서만 막는 게 아니라 UI에서도 불가능한 동작을 노출하지 않습니다. 그러면 사용자 입장에서도 혼란이 없고, 개발자 입장에서도 의도치 않은 API 호출을 줄일 수 있습니다."

---

### A-5. 공연 심사 — 미리보기 후 승인 → 게시

→ **왼쪽 메뉴에서 공연 관리 클릭 또는 대시보드의 심사 대기 탭 클릭**

"공연 심사 화면입니다. 여기에 REVIEW 상태인 공연들이 목록으로 나타납니다. 기획사가 심사 요청 버튼을 눌러서 DRAFT에서 REVIEW로 전환한 공연들입니다.

공연 FSM도 설명드리면, DRAFT — 작성 중, REVIEW — 심사 대기, APPROVED — 승인됨, PUBLISHED — 판매 중, REJECTED — 반려. 이 순서로 진행됩니다."

→ **심사 대기 공연 목록에서 한 개 선택 → 미리보기 버튼 클릭**

"미리보기 버튼을 눌렀습니다. 모달이 떴습니다.

11차 세션 이전에는 이 미리보기 모달이 없었습니다. 그냥 목록에서 승인 버튼만 있었습니다. 그런데 생각해보면 공연 내용을 확인하지도 않고 바로 승인하는 건 말이 안 됩니다. 가격이 잘못 입력됐거나, 일정이 중복되거나, 포스터가 부적절하거나 하는 경우를 걸러야 하니까요. 그래서 미리보기를 의무 단계로 추가했습니다."

→ **모달 천천히 스크롤하며 내용 설명**

"모달에서 공연 기본 정보, 공연회차 일정, 공연장 구역별 좌석 등급과 가격 정보를 전부 확인할 수 있습니다.

이 데이터를 가져오는 API는 /backoffice/super/api/performances/{id}/detail입니다. 컨트롤러에서 Performance, Schedule, Venue, SeatGrade 정보를 한꺼번에 조회해서 내려줍니다. 11차 세션에서 이 통합 조회 API와 모달 UI를 함께 추가했습니다."

→ **모달 닫기 → 같은 공연 행의 승인 버튼 클릭**

"승인했습니다. REVIEW에서 APPROVED로 전환됐습니다.

APPROVED는 '어드민이 내용을 확인하고 통과시킨 상태'입니다. 그런데 아직 사용자 화면에는 이 공연이 안 보입니다. 판매를 시작하려면 PUBLISHED가 돼야 합니다.

왜 APPROVED와 PUBLISHED를 분리했냐면, 승인 후 바로 판매가 시작되면 기획사나 어드민이 오픈 날짜를 조율할 수 없기 때문입니다. 예를 들어 오늘 공연을 승인했지만 실제 예매 오픈은 다음 주 월요일 오전 10시로 잡고 싶다면, APPROVED 상태로 유지하다가 오픈 시점에 게시 버튼을 누르면 됩니다."

→ **APPROVED된 공연 행 → 게시 버튼 클릭**

"게시 버튼을 눌렀습니다. PUBLISHED 상태가 됐습니다. 이제 사용자 포털에서 이 공연이 보이고 예매가 가능합니다.

PUBLISHED는 터미널 상태입니다. 게시 취소 버튼이 없습니다. 공연이 이미 판매 시작됐는데 임의로 내리면 이미 예매한 사람들에게 큰 혼란이 생기기 때문입니다. 게시 이후에 공연을 내리려면 별도의 취소 공지와 전체 환불 프로세스가 필요한데, 이건 Phase 3~4에서 다룰 예정입니다."

---
---

# 시연 2 — 파트너 포털
## 목표: 로그인 → 대시보드 → 공연 등록 → 회차 추가 → 좌석 등급 설정 → 심사 요청

---

### B-1. 파트너 로그인

→ **탭 B 클릭 (localhost:8081/partner/login)**

"파트너 포털입니다. 포트 8081번입니다. 기획사 담당자와 공연장 담당자가 쓰는 화면입니다.

사용자 포털 8080과 결정적으로 다른 점이 있습니다. 사용자 포털은 누구나 회원가입 버튼이 있어서 스스로 계정을 만들 수 있습니다. 하지만 파트너 포털은 회원가입 버튼 자체가 없습니다. 어드민이 어드민 포털에서 계정을 생성하고, 어드민이 승인해야만 로그인이 가능합니다.

조금 전 어드민에서 promoter2를 승인했는데, 그게 바로 이 문을 열어준 겁니다. 승인 전이었으면 아무리 ID와 비밀번호를 맞게 입력해도 로그인이 거절됩니다."

→ **이메일 → `promoter1@test.com` 입력**

→ **비밀번호 → `Cks159753!` 입력**

→ **로그인 버튼 클릭**

"로그인됐습니다."

→ **F12 → Application → Cookies → localhost**

"쿠키 목록을 보시면 이제 ADMIN_TOKEN과 PARTNER_TOKEN 두 개가 함께 있습니다. 어드민 탭과 파트너 탭이 각자 자기 쿠키를 가지고 있고, 서로를 덮어쓰지 않습니다. 이름이 다르니까요.

지금 이 브라우저에서 어드민 탭으로 가면 어드민으로 로그인된 상태고, 이 파트너 탭은 파트너로 로그인된 상태입니다. 두 상태가 동시에 유지됩니다."

→ **F12 닫기**

---

### B-2. 파트너 대시보드

"파트너 대시보드입니다. 내가 등록한 공연 목록과 각 공연 상태가 보입니다."

→ **대시보드 공연 목록 보여주기**

"공연 상태가 색으로 구분됩니다. DRAFT는 회색, REVIEW는 주황색, APPROVED는 초록색, PUBLISHED는 파란색, REJECTED는 빨간색입니다.

기획사 입장에서 자기 공연이 지금 어드민 심사 중인지, 승인됐는지, 아직 임시 저장 중인지 한눈에 파악할 수 있습니다.

REJECTED된 공연이 있으면 빨간색으로 표시됩니다. 기획사가 이걸 클릭하면 반려 사유가 보이고, 수정 후 다시 심사 요청을 할 수 있습니다. REJECTED에서 다시 DRAFT로 내려와서 수정 가능한 상태가 되는 겁니다."

---

### B-3. 신규 공연 등록 (DRAFT 생성)

→ **공연 관리 메뉴 클릭 → 공연 등록 버튼 클릭**

"공연 등록 폼입니다. 지금부터 새 공연을 등록해보겠습니다.

등록 버튼을 누르는 순간 이 공연은 DRAFT 상태로 저장됩니다. DRAFT는 기획사가 아직 작성 중인 상태입니다. 외부에 공개되지 않고, 어드민 심사 대기 목록에도 올라가지 않습니다. 기획사 본인만 볼 수 있습니다."

→ **공연명 입력창 클릭 → `[LIVE 시연] 봄날의 콘서트 2026` 입력**

"공연 이름을 입력했습니다. 지금 이 공연 이름이 이후 어드민 심사 목록, 사용자 공연 목록에 그대로 보입니다."

→ **카테고리 드롭다운 클릭 → 콘서트 선택**

→ **공연장 드롭다운 클릭 → 아무 공연장 하나 선택**

"공연장 선택이 드롭다운으로 되어 있습니다. 기획사가 직접 공연장 이름을 타이핑하는 게 아니라, DB에 등록된 공연장 중에서 선택하도록 제한되어 있습니다. 존재하지 않는 공연장에서 공연을 열 수는 없으니까요.

공연장 정보는 venue 테이블에 있고, 각 공연장에는 구역 정보가 연결되어 있습니다. venue_section 테이블에 공연장별 구역 레이아웃이 들어 있습니다."

→ **공연 시작 날짜 → 약 2개월 후 날짜 선택**

→ **공연 설명 입력 → `TicketLegacy 라이브 시연용 공연입니다. Spring MVC 5.3 기반 공연 예매 플랫폼의 전체 워크플로우를 시연합니다.` 입력**

"공연 설명입니다. DB에는 HTML 형식으로 저장됩니다. 그래서 나중에 Elasticsearch에 인덱싱할 때 HTML 태그를 파싱해서 검색 텍스트를 추출하는 방식으로 전문 검색을 구현할 수 있습니다. 이미 데이터 형식을 Elasticsearch 연동 준비에 맞춰 설계해두었습니다."

→ **등록 버튼 클릭**

"등록됐습니다. 공연 목록으로 이동해보면 방금 만든 공연이 DRAFT 상태로 보입니다."

→ **공연 목록 확인 — DRAFT 공연 표시 확인**

"DRAFT 상태입니다. 어드민 탭을 열어도 이 공연은 심사 목록에 없습니다. 기획사가 심사 요청 버튼을 직접 눌러야만 REVIEW로 전환되고 어드민에게 보입니다."

---

### B-4. 공연 회차 추가

→ **등록된 공연 클릭 → 상세 페이지 → 회차 추가 또는 일정 관리 버튼 클릭**

"공연 상세 페이지에서 공연 회차를 추가합니다.

하나의 공연이 여러 날짜, 여러 회차를 가질 수 있습니다. 예를 들어 BTS 콘서트가 잠실에서 3일간 공연하면 공연 1개에 회차가 3개입니다. 각 회차는 performance_schedule 테이블에 별도 row로 저장됩니다.

중요한 것은 각 회차가 독립적인 좌석 인벤토리를 가진다는 점입니다. 1회차 A7번 좌석이 예매됐어도 2회차 A7번은 여전히 예매 가능합니다."

→ **날짜 선택 → 시작 시간 14:00 입력 → 종료 시간 17:00 입력**

→ **추가 버튼 클릭**

"1회차가 추가됐습니다. 좌석 등급을 설정하고 나면 이 회차에 대한 좌석 인벤토리가 자동으로 생성됩니다."

→ **한 회차 더 추가 → 날짜 동일, 시간 19:00 ~ 22:00**

"2회차도 추가했습니다. 같은 날 낮 공연과 저녁 공연처럼 한 날에 두 회차도 가능합니다."

---

### B-5. 좌석 등급 및 가격 설정

→ **좌석 등급 설정 버튼 클릭 또는 해당 메뉴 이동**

"좌석 등급 설정 화면입니다.

공연장에는 여러 구역이 있고, 구역마다 다른 좌석 등급과 가격을 설정할 수 있습니다. 예를 들어 1층 앞 구역은 VIP석 15만원, 1층 뒤 구역은 R석 9만9천원, 2층은 S석 5만5천원 이런 식으로요.

이 데이터는 performance_seat_grade 테이블에 저장됩니다. performance_id, section_id, grade_name, price 조합으로 저장되어서 공연마다 같은 공연장이라도 가격이 다를 수 있습니다."

→ **구역 드롭다운 → 1층 VIP 구역 선택**

→ **등급명 입력창 → `VIP` 입력**

→ **가격 입력창 → `150000` 입력**

→ **추가 버튼 클릭**

"VIP석 15만원이 설정됐습니다."

→ **구역 드롭다운 → 다른 구역 선택**

→ **등급명 → `R석` → 가격 → `99000` → 추가**

"R석 9만9천원."

→ **구역 드롭다운 → 또 다른 구역 선택**

→ **등급명 → `S석` → 가격 → `55000` → 추가**

"S석 5만5천원. 이 세 가지 등급이 설정됐습니다.

이 가격 정보가 나중에 사용자 포털 좌석 선택 화면에 구역별로 표시됩니다. 좌석을 선택하면 해당 구역의 등급 가격이 자동으로 결제 금액에 반영됩니다."

---

### B-6. 심사 요청 — DRAFT에서 REVIEW로 전환

→ **공연 상세 페이지 → 심사 요청 버튼 찾기**

"이제 준비가 됐으니 심사 요청을 하겠습니다. 이 버튼을 누르면 공연 상태가 DRAFT에서 REVIEW로 전환됩니다."

→ **심사 요청 버튼 클릭 → 확인 다이얼로그 → 확인**

"심사 요청됐습니다."

→ **상태 표시 확인 — REVIEW로 바뀜**

"상태가 REVIEW로 바뀌었습니다. 파트너 입장에서는 이제 어드민의 응답을 기다리는 단계입니다.

REVIEW 상태에서는 공연 정보를 수정할 수 없습니다. 수정 버튼이 비활성화됩니다. 어드민이 검토하는 중에 내용이 바뀌면 어드민이 확인한 내용과 달라질 수 있기 때문입니다. 만약 수정이 필요하면 어드민에게 반려 요청을 해서 DRAFT로 내려온 다음 수정해야 합니다."

→ **어드민 탭으로 이동 — 탭 A 클릭**

"어드민 탭으로 이동합니다. 방금 심사 요청한 공연이 어드민 대시보드에 나타났는지 확인하겠습니다."

→ **대시보드 새로고침 또는 공연 관리 탭 클릭**

"여기 심사 대기 목록에 '[LIVE 시연] 봄날의 콘서트 2026'이 올라왔습니다. 방금 파트너에서 심사 요청 버튼을 누르자마자 어드민 대시보드에 실시간으로 반영됐습니다."

→ **미리보기 버튼 클릭 → 내용 확인 → 닫기 → 승인 → APPROVED → 게시 → PUBLISHED**

"이미 A-5에서 보여드린 흐름이라 간단히 진행하겠습니다. 미리보기 확인 — 승인 — 게시. PUBLISHED가 됐습니다. 이제 사용자 포털에서 예매 가능합니다."

---
---

# 시연 3 — 사용자 포털
## 목표: 로그인 → 공연 탐색 → 좌석 선택(Redis 선점) → 결제(트랜잭션) → 예매 확인 → 취소(좌석 복원)

---

### C-1. 사용자 로그인

→ **탭 C 클릭 (localhost:8080/member/login)**

"사용자 포털입니다. 포트 8080번입니다. 일반 사용자가 공연을 찾고 예매하는 화면입니다.

여기는 어드민 포털과 다르게 회원가입 링크가 있습니다. 누구나 스스로 계정을 만들 수 있습니다. POST /api/member/join 엔드포인트로 이메일과 비밀번호를 보내면 계정이 생성됩니다. 비밀번호는 서버에서 BCrypt로 해싱해서 저장합니다."

→ **이메일 → `user1@test.com` 입력**

→ **비밀번호 → `Cks159753!` 입력**

→ **로그인 버튼 클릭**

"로그인됐습니다. 이제 브라우저에 USER_TOKEN이 추가됩니다. 지금 이 브라우저에 ADMIN_TOKEN, PARTNER_TOKEN, USER_TOKEN 세 개가 동시에 존재합니다. 어드민, 파트너, 사용자로 동시에 로그인된 상태입니다."

→ **F12 → Application → Cookies 잠깐 보여주기**

"쿠키가 세 개 있습니다. 각자 다른 이름, 다른 JWT 토큰, 다른 역할을 담고 있습니다. 이 구조 덕분에 개발 시 세 포털을 동시에 테스트할 수 있어서 매우 편리했습니다."

→ **F12 닫기**

---

### C-2. 공연 목록 탐색

→ **메인 페이지 또는 공연 목록 페이지로 이동**

"공연 목록 화면입니다. PUBLISHED 상태, 즉 판매 중인 공연들이 보입니다.

공연이 200개가 넘습니다. 이 데이터를 어떻게 만들었는지 잠깐 설명드리겠습니다. DB 시드 스크립트인 data_extension.sql에 WITH RECURSIVE CTE를 활용했습니다.

WITH RECURSIVE는 MySQL 8에서 지원하는 재귀 공통 테이블 표현식입니다. 재귀 구조로 공연에 연결된 모든 하위 데이터를 한 번에 생성합니다. 공연 하나를 만들면 그에 딸린 공연회차 4개, 각 회차에 딸린 좌석 등급 3개, 각 등급에 딸린 좌석 60개, 각 좌석에 딸린 인벤토리가 자동으로 생성됩니다. 그 결과 공연 214개에 좌석 인벤토리가 12,840개 이상입니다. 수동으로 입력했으면 며칠이 걸릴 데이터를 스크립트 한 번에 만들었습니다."

→ **공연 카드 포스터 이미지 가리키며**

"포스터 이미지는 picsum.photos라는 무료 이미지 서비스를 사용했습니다. URL 뒤에 seed 파라미터로 공연 ID를 넣으면 항상 같은 이미지가 나옵니다. 공연 5번은 항상 같은 이미지, 공연 6번은 항상 다른 이미지. 실제 서비스처럼 각 공연이 고유한 포스터를 가진 것처럼 보입니다."

→ **방금 등록하고 게시한 '[LIVE 시연] 봄날의 콘서트 2026' 찾기**

"방금 파트너에서 등록하고 어드민에서 게시한 공연이 여기 보입니다. 파트너 포털에서 공연 등록 → 심사 요청 → 어드민 미리보기 → 승인 → 게시. 이 전체 플로우가 방금 몇 분 만에 실시간으로 이루어졌고, 결과가 바로 이 화면에 반영됩니다."

---

### C-3. 공연 상세 페이지

→ **해당 공연 카드 클릭**

"공연 상세 페이지입니다.

공연 기본 정보, 포스터, 공연 설명, 공연회차 목록이 표시됩니다."

→ **좌석 등급별 가격 정보 영역 가리키기**

"좌석 등급별 가격이 표시됩니다. VIP 150,000원, R석 99,000원, S석 55,000원. 파트너 포털에서 설정한 값이 그대로 나옵니다."

→ **공연회차 목록 보여주기**

"회차 목록입니다. 방금 추가한 1회차 오후 2시, 2회차 오후 7시 공연이 표시됩니다.

각 회차를 클릭하면 해당 회차의 좌석 현황을 볼 수 있습니다. 회차마다 독립적인 seat_inventory를 가지기 때문에 1회차 A7번이 예매됐어도 2회차 A7번은 예매 가능합니다."

→ **1회차 클릭**

---

### C-4. 좌석 선택 — Redis Lua 원자적 선점

→ **예매하기 버튼 클릭 또는 좌석 선택 화면 진입**

"좌석 선택 화면으로 들어왔습니다.

좌석이 구역별로 표시됩니다. AVAILABLE 상태는 선택 가능, HOLD 상태는 다른 누군가가 임시 선점 중, RESERVED는 예매 완료입니다."

→ **AVAILABLE 상태인 좌석 하나 클릭**

"좌석을 선택했습니다. 이 순간 서버로 선점 요청이 전송됩니다.

서버에서 무슨 일이 일어나는지 설명드리겠습니다. 서버는 Redis에 Lua 스크립트를 전송합니다. 이 Lua 스크립트는 Redis 서버에서 실행됩니다. 스크립트 내용은 이렇습니다. 해당 좌석 키의 현재 값이 AVAILABLE이면 HOLD로 바꾸고 TTL 600초를 설정한 다음 성공을 반환. 만약 AVAILABLE이 아니면 아무것도 바꾸지 않고 실패를 반환.

이 두 동작이 원자적으로 실행됩니다. 원자적이라는 건 이 스크립트가 실행되는 동안 다른 요청이 끼어들 수 없다는 뜻입니다. Redis는 기본적으로 싱글 스레드로 명령을 처리하고, Lua 스크립트는 하나의 명령으로 취급됩니다.

그래서 동시에 100명이 같은 좌석 선택 버튼을 눌러도, Redis 서버에서는 순차적으로 처리됩니다. 가장 먼저 도착한 요청 하나만 AVAILABLE → HOLD 전환에 성공하고, 나머지 99개는 이미 HOLD 상태를 보고 실패를 반환받습니다."

→ **F12 → Network 탭 → 좌석 선점 요청 응답 확인**

"네트워크 탭에서 선점 요청 응답을 보시면 success: true가 반환됐습니다. 좌석이 정상적으로 HOLD됐습니다.

그리고 이 선점은 10분짜리입니다. 10분 안에 결제를 완료하지 않으면 서버의 @Scheduled 배치가 3초마다 실행되면서 만료된 HOLD 좌석을 AVAILABLE로 되돌립니다. 실제 티켓 구매 시 '10분 안에 결제하세요' 메시지가 뜨는 게 바로 이 원리입니다."

→ **F12 닫기**

"좌석 색상이 HOLD를 나타내는 색으로 바뀐 것이 보이시죠. 이 좌석은 10분간 다른 사람이 선택할 수 없습니다."

---

### C-5. 동시 선점 충돌 직접 시연

"동시 접근 상황을 직접 보여드리겠습니다."

→ **새 시크릿 창 열기 (Ctrl+Shift+N)**

→ **localhost:8080/member/login 접속 → user2@test.com / Cks159753! 로그인**

→ **같은 공연, 같은 회차, 방금 선점한 같은 좌석 번호 찾기**

"다른 사용자로 로그인해서 방금 선점된 좌석을 클릭해보겠습니다."

→ **이미 HOLD된 좌석 클릭 시도**

"응답을 보시면 '이미 다른 고객이 선점한 좌석입니다' 또는 '좌석 선점에 실패했습니다' 메시지가 나옵니다. 중복 선점이 차단됩니다.

Lua 스크립트가 HOLD 상태를 보고 실패를 반환했기 때문입니다. DB 쿼리조차 실행되지 않았습니다."

→ **시크릿 창 닫기 → 원래 탭으로 복귀**

---

### C-6. 결제 처리 — 트랜잭션 3중 처리

→ **결제 진행 버튼 클릭 → 결제 페이지 이동**

"결제 페이지입니다. 선택한 좌석 정보와 금액이 표시됩니다.

화면 상단에 남은 시간 카운트다운이 표시됩니다. 10분 타이머입니다. 이게 만료되면 선점이 해제되고 처음부터 다시 해야 합니다."

→ **쿠폰 영역 보여주기 (있는 경우)**

"쿠폰 영역이 있습니다. 현재 쿠폰 조회까지는 구현되어 있고, 선택한 쿠폰의 할인 금액을 결제에 적용하는 UI는 Phase 4에서 완성 예정입니다."

→ **결제 버튼 클릭**

"결제 버튼을 눌렀습니다. 서버에서 지금 세 가지 작업이 하나의 @Transactional 블록 안에서 처리됩니다.

첫 번째. payment 테이블에 결제 레코드 INSERT. 이때 idempotency_key도 함께 저장됩니다. idempotency_key는 이 결제 요청을 유일하게 식별하는 값입니다. UNIQUE 제약이 걸려 있어서 같은 key로 두 번 INSERT하면 DB 오류가 납니다.

두 번째. reservation 상태를 PENDING에서 CONFIRMED로 UPDATE합니다. 예약이 확정됩니다.

세 번째. seat_inventory 상태를 HOLD에서 RESERVED로 UPDATE합니다. 좌석이 완전히 예약됩니다.

이 세 가지가 모두 하나의 트랜잭션입니다. 도중에 하나라도 실패하면 전체가 롤백됩니다. 결제 레코드는 생겼는데 예약 상태가 안 바뀌거나, 예약은 됐는데 좌석이 잡히지 않는 데이터 불일치 상황이 발생하지 않습니다."

→ **결제 완료 화면 표시**

"결제가 완료됐습니다. 예매 확인 번호가 발급됐습니다.

결제 완료 메시지가 보이는 이 순간, DB에서는 세 테이블이 모두 업데이트된 상태입니다."

---

### C-7. idempotency_key — 중복 결제 방지

"idempotency_key가 왜 필요한지 시나리오로 설명드리겠습니다.

결제 버튼을 눌렀는데 인터넷이 잠깐 느려서 서버 응답이 5초가 걸립니다. 사용자는 반응이 없으니까 버튼을 한 번 더 누릅니다. 이제 서버에 같은 결제 요청이 두 번 들어왔습니다.

첫 번째 요청이 결제 처리를 시작했습니다. payment 테이블에 INSERT 시도. 성공합니다.

두 번째 요청도 결제 처리를 시작합니다. payment 테이블에 같은 idempotency_key로 INSERT 시도. UNIQUE 제약에 걸려서 DB 오류가 납니다. 서버가 이 오류를 잡아서 '이미 처리된 결제입니다'로 응답합니다.

결과적으로 같은 예매에 대해 결제가 한 번만 이루어집니다. 네트워크 불안정이나 사용자 실수로 인한 중복 결제를 DB 제약 하나로 원천 차단합니다."

---

### C-8. 마이페이지 예매 내역

→ **헤더의 마이페이지 또는 예매 내역 링크 클릭**

"마이페이지 예매 내역입니다. 방금 완료한 예매가 CONFIRMED 상태로 보입니다.

좌석 번호, 공연명, 공연 일시, 결제 금액이 모두 표시됩니다."

→ **예매 상세 클릭 (있는 경우)**

"상세를 보면 결제 정보도 확인할 수 있습니다. 결제 방식, 결제 시각, 예매 번호가 나옵니다."

---

### C-9. 예매 취소 — 좌석 복원 시연

→ **예매 내역에서 방금 예매한 항목 → 취소 버튼 클릭**

"취소 버튼을 눌렀습니다."

→ **SweetAlert 확인 다이얼로그 등장**

"'예매를 취소하시겠습니까? 취소 후에는 되돌릴 수 없습니다.' 확인 다이얼로그가 뜹니다. 실수로 취소하는 것을 방지하는 처리입니다."

→ **확인 클릭**

"취소됐습니다. 상태가 CONFIRMED에서 CANCELLED로 전환됩니다. 동시에 seat_inventory가 RESERVED에서 AVAILABLE로 복원됩니다.

이제 다른 사용자가 이 좌석을 예매할 수 있습니다. 취소 직후 좌석 선택 화면으로 가보면 이 좌석이 다시 선택 가능한 상태로 표시됩니다."

→ **공연 상세 → 좌석 선택 화면으로 이동하여 해당 좌석 AVAILABLE 확인**

"보이시죠. 방금 취소한 좌석이 다시 AVAILABLE 상태가 됐습니다. 좌석이 정확하게 복원됐습니다.

중요한 점은 CANCELLED와 REFUNDED가 다르다는 겁니다. CANCELLED는 예매 상태 변경입니다. 실제 결제 금액의 환불은 PG사에 취소 요청을 보내야 하는 별도 프로세스입니다. PG사 연동 후 환불이 완료되면 REFUNDED 상태로 전환됩니다. 현재 PG가 mock이라 이 부분은 Phase 3에서 구현 예정입니다."

---
---

# 시연 4 — 기술 심화
## 목표: FSM 위반 API 직접 확인 → GlobalExceptionHandler 원리 → 코드 구조 설명

---

### D-1. FSM 위반 — 직접 API 호출

→ **어드민 탭 클릭 → F12 → Console 탭**

"API를 직접 호출해서 FSM 위반을 시도해보겠습니다.

PUBLISHED 상태인 공연을 DRAFT로 되돌리려고 합니다. PUBLISHED는 터미널 상태입니다. 코드 어디서 시도해도 거절됩니다."

→ **Console에 입력 → 엔터:**

```javascript
fetch('/backoffice/super/api/performances/1/rollback-to-draft', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' }
}).then(r => r.json()).then(d => console.log(JSON.stringify(d, null, 2)))
```

"응답이 왔습니다. 결과를 읽어보겠습니다."

→ **응답 결과 가리키며 천천히 읽기**

"success: false. errorCode: PERF_006. message: '허용되지 않는 상태 전환입니다.' HTTP 상태코드는 400입니다.

이 응답이 만들어지는 과정을 단계별로 설명드리겠습니다.

1단계. 컨트롤러가 서비스 메서드를 호출합니다.
2단계. 서비스가 PerformanceApprovalStatus.PUBLISHED.validateTransition(DRAFT)를 호출합니다.
3단계. PUBLISHED는 canTransitionTo()에서 아무것도 허용하지 않으므로 false를 반환합니다.
4단계. 서비스가 new BusinessException(ErrorCode.PERF_006)을 던집니다.
5단계. GlobalExceptionHandler가 @ExceptionHandler(BusinessException.class)로 이 예외를 잡습니다.
6단계. ApiResponse.error(exception.getErrorCode())로 응답 객체를 만들어서 400으로 반환합니다.

이 6단계가 몇 밀리초 안에 일어납니다."

---

### D-2. 존재하지 않는 회원 요청

→ **Console에 입력:**

```javascript
fetch('/backoffice/super/api/members/99999/status', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ status: 'SUSPENDED' })
}).then(r => r.json()).then(d => console.log(JSON.stringify(d, null, 2)))
```

"ID가 99999인 회원은 존재하지 않습니다. 이 요청이 어떻게 처리되는지 보겠습니다."

→ **응답 확인**

"errorCode: MBR_001. message: '회원을 찾을 수 없습니다.' HTTP 404입니다.

에러 코드 체계를 보시면 MBR은 Member 도메인, 001은 찾을 수 없음을 의미합니다. PRO는 Promoter, VM은 VenueManager, PERF는 Performance. 숫자는 001이 NOT_FOUND, 005가 FSM 위반 이런 식으로 규칙이 있습니다. 클라이언트 개발자가 에러 코드만 보고 어떤 문제인지 바로 파악할 수 있습니다."

→ **F12 닫기**

---

### D-3. VS Code에서 코드 구조 설명

→ **VS Code 또는 STS 열기**

---

#### D-3-1. MemberStatus Enum

→ **ticket-common/src/main/java/.../domain/enums/MemberStatus.java 열기**

"MemberStatus Enum입니다. FSM의 핵심 파일입니다.

canTransitionTo() 메서드를 보시면 switch문으로 각 상태가 어디로 갈 수 있는지 명시되어 있습니다.

ACTIVE는 SUSPENDED, WITHDRAWN, DORMANT로 갈 수 있습니다.
SUSPENDED는 ACTIVE, WITHDRAWN으로 갈 수 있습니다.
DORMANT는 ACTIVE로만 갈 수 있습니다.
WITHDRAWN은 case가 비어 있습니다. 아무 데도 못 갑니다. 터미널 상태입니다.
PENDING_APPROVAL은 ACTIVE, SUSPENDED로 갈 수 있습니다.

이 규칙이 바뀌면 이 메서드 하나만 수정하면 됩니다. 서비스 코드, 컨트롤러 코드는 건드릴 필요가 없습니다."

---

#### D-3-2. MemberService

→ **ticket-common/src/main/java/.../service/MemberService.java 열기 → updateAdminStatus() 메서드 찾기**

"updateAdminStatus() 메서드입니다. 회원 상태를 변경하는 서비스 메서드입니다.

코드를 보시면 핵심 로직이 굉장히 짧습니다. 먼저 회원을 조회합니다. 없으면 MBR_001 예외. 다음에 canTransitionTo()를 호출합니다. false면 MBR_005 예외. 통과하면 mapper.updateStatus()로 DB UPDATE.

비즈니스 로직이 복잡하지 않습니다. Enum이 규칙을 담당하고, 서비스는 그 규칙을 호출하기만 합니다. 이렇게 책임을 분리하는 것이 좋은 설계입니다."

---

#### D-3-3. GlobalExceptionHandler

→ **ticket-admin/src/main/java/.../web/exception/GlobalExceptionHandler.java 열기**

"GlobalExceptionHandler입니다. @ControllerAdvice 어노테이션이 있어서 이 컨트롤러가 모든 예외를 가로챕니다.

@ExceptionHandler(BusinessException.class)로 BusinessException이 발생하면 이 메서드가 실행됩니다. exception.getErrorCode()로 에러 코드 Enum을 꺼내고, ApiResponse.error()로 응답 객체를 만들고, ResponseEntity에 담아서 반환합니다.

이 파일이 세 WAR에 동일하게 존재합니다. ticket-user, ticket-partner, ticket-admin 모두 같은 형식으로 오류를 응답합니다. API 사용자 입장에서는 어느 포털에서 오류가 나도 응답 형식이 같으니 파싱 로직을 하나로 통일할 수 있습니다."

---

#### D-3-4. ApiResponse

→ **ticket-common/src/main/java/.../dto/ApiResponse.java 열기**

"ApiResponse입니다. 모든 API 응답의 래퍼 클래스입니다.

success, message, data, errorCode 네 필드로 구성됩니다.

성공 시에는 ApiResponse.success(data)로 만듭니다. success: true, data에 실제 데이터.
오류 시에는 ApiResponse.error(errorCode)로 만듭니다. success: false, errorCode에 에러 코드.

이 클래스가 ticket-common에 있기 때문에 세 WAR가 공유합니다. 세 포털의 응답 형식이 항상 일관성을 유지합니다."

---

#### D-3-5. JwtAuthenticationFilter

→ **ticket-admin/src/main/java/.../web/filter/JwtAuthenticationFilter.java 열기**

"JwtAuthenticationFilter입니다. 모든 HTTP 요청에서 가장 먼저 실행되는 필터입니다.

요청 쿠키에서 ADMIN_TOKEN을 꺼냅니다. 없으면 인증 정보 없이 통과. 있으면 JJWT로 서명 검증합니다. 유효하면 토큰에서 userId와 role을 꺼내서 SecurityContext에 설정합니다.

이 필터가 실행된 이후에 Spring Security가 security-context.xml의 URL 패턴 규칙을 적용합니다. SecurityContext에 SUPER_ADMIN 역할이 있으면 /backoffice/super/** 접근 허용, 없으면 403 반환.

이 파일도 세 WAR에 각각 있습니다. 차이는 쿠키 이름뿐입니다. ticket-user는 USER_TOKEN, ticket-partner는 PARTNER_TOKEN, ticket-admin은 ADMIN_TOKEN을 읽습니다."

---

### D-4. 세 포털 역할 기반 접근 제어 확인

→ **어드민 탭의 주소창에 `localhost:8080/mypage` 입력**

"어드민 계정으로 사용자 포털의 마이페이지에 접근을 시도합니다. 어드민 JWT가 ADMIN_TOKEN 쿠키에 있는 상태입니다."

→ **결과 화면 보여주기**

"리다이렉트되거나 접근이 차단됩니다. 어드민 JWT에는 SUPER_ADMIN 역할이 담겨 있는데, ticket-user의 security-context.xml에서 마이페이지 접근에는 MEMBER 역할이 필요하기 때문입니다.

JWT 서명이 올바르고 만료도 안 됐어도, 역할이 맞지 않으면 접근이 안 됩니다. 토큰 위조 방지와 역할 기반 접근 제어는 별개의 레이어입니다. 두 레이어가 각자 역할을 수행합니다."

---
---

# 시연 마무리

"이상으로 TicketLegacy 라이브 시연을 마치겠습니다.

지금 보여드린 것들을 정리하면 이렇습니다.

어드민 포털에서는 JWT HttpOnly 쿠키, FSM 기반 기획사·회원 상태 관리, 공연 미리보기와 심사·게시 워크플로우를 확인했습니다.

파트너 포털에서는 공연 등록부터 회차 추가, 좌석 등급 가격 설정, 심사 요청까지의 전체 플로우를 봤습니다.

사용자 포털에서는 Redis Lua 원자적 선점, 동시 접근 충돌 차단, 트랜잭션 기반 결제 확정, 좌석 복원까지 예매 전체 사이클을 시연했습니다.

기술 심화에서는 FSM 위반 API를 직접 호출해서 에러 응답을 확인하고, 코드 구조에서 Enum-Service-Handler 흐름을 설명드렸습니다.

질문 있으시면 편하게 말씀해 주세요."

---
---

# Q&A 예상 질문 (시연 관련)

---

**Q. 동시에 두 명이 정확히 같은 순간에 같은 좌석을 선택하면 Redis가 진짜 한 명만 허용하나요?**

"네, 보장됩니다. Redis는 싱글 스레드 이벤트 루프로 명령을 처리합니다. Lua 스크립트는 하나의 원자 명령으로 취급됩니다. 두 요청이 네트워크를 통해 Redis 서버에 동시에 도달해도, Redis 내부에서는 반드시 하나씩 순차적으로 처리됩니다. 첫 번째 스크립트가 AVAILABLE → HOLD로 바꾸고 완료되면, 두 번째 스크립트가 실행될 때는 이미 HOLD 상태이므로 실패를 반환합니다. 물리적으로 둘 다 성공하는 건 불가능합니다."

---

**Q. Redis가 다운되면 예매 자체가 안 되나요?**

"서비스는 계속됩니다. Redis 선점 요청이 실패하면 try-catch에서 잡아서, Redis 없이 DB 직접 UPDATE로 폴백합니다. WHERE status='AVAILABLE' 조건부 UPDATE가 두 번째 방어선이 되어 중복 예매를 막습니다. 다만 Redis가 없으면 이 쿼리가 경쟁 상태에 놓이기 때문에 순간적인 중복 가능성이 생깁니다. 성능도 저하됩니다. 그래서 Redis는 필수 인프라로 관리합니다."

---

**Q. PUBLISHED 공연을 강제로 내릴 수 있는 방법이 없나요? 잘못 게시한 경우에는 어떻게 하나요?**

"현재 코드상으로는 PUBLISHED → DRAFT 롤백이 FSM에서 막혀 있습니다. 이것은 의도적인 설계입니다. 공연이 게시되면 사용자들이 예매를 시작하기 때문에, 임의로 내리면 기존 예매자들에게 큰 혼란이 생깁니다. 실제 서비스에서는 공연 취소 공지, 전체 예매자 환불, 취소 처리가 모두 이루어진 후에만 공연을 내릴 수 있도록 별도 프로세스가 필요합니다. Phase 3~4에서 이 플로우를 구현할 예정입니다."

---

**Q. 세 포털이 같은 DB를 쓰는데 트랜잭션 충돌은 없나요?**

"각 서버가 HikariCP 커넥션 풀을 독립적으로 가집니다. 같은 DB에 여러 커넥션이 연결되는 형태입니다. MySQL의 InnoDB가 트랜잭션과 행 수준 잠금을 처리합니다. 어드민이 공연 상태를 변경하는 동안 사용자가 같은 공연의 좌석을 예매하는 것은 서로 다른 테이블 행을 건드리기 때문에 충돌이 없습니다. 같은 좌석에 동시 접근하는 경우만 Redis + DB WHERE 조건으로 처리합니다."

---

**Q. 쿠폰 기능이 있다고 했는데 실제로 적용이 되나요?**

"쿠폰 템플릿 생성, 쿠폰 발급, 발급된 쿠폰 조회까지는 구현되어 있습니다. 결제 화면에서 보유 쿠폰 목록이 나오는 것도 됩니다. 하지만 쿠폰을 선택하면 할인 금액이 실시간으로 반영되고, 최종 결제 시 할인가로 처리되는 UI 연동은 Phase 4에서 완성 예정입니다. 서버 API는 준비되어 있고 프론트엔드 연결이 남아 있는 상태입니다."

---

**Q. 대기열 시스템이 있다고 했는데 어떻게 동작하나요?**

"QueueInterceptor가 ticket-user에 있습니다. 특정 공연에 동시 접속자가 급증하면 인터셉터가 요청을 가로채서 대기열에 넣습니다. @Scheduled로 3초마다 대기열에서 순서대로 입장을 허용합니다. 사용자 화면에는 현재 대기 순번과 예상 대기 시간이 표시됩니다. 현재 기본 동작은 구현되어 있고, 대기열 임계값과 처리 속도 튜닝은 실제 트래픽 데이터를 보고 조정해야 합니다."

---

**Q. MyBatis를 쓰면 N+1 문제가 없나요?**

"MyBatis에서도 N+1은 발생할 수 있습니다. 저희는 resultMap에 association과 collection을 활용해서 JOIN 쿼리로 한 번에 연관 데이터를 조회하는 방식으로 처리했습니다. 예를 들어 공연 목록 조회 시 공연장 정보를 별도 쿼리로 N번 조회하지 않고, 처음 쿼리에서 JOIN으로 함께 가져옵니다. 성능에 민감한 쿼리는 실행 계획을 직접 확인하면서 최적화했습니다."

---

**Q. 지금 당장 실제 서비스로 런칭할 수 있나요?**

"솔직히 말씀드리면 두 가지가 없어서 불가능합니다. 첫째, 결제 PG 실제 연동이 없습니다. 지금은 결제가 항상 성공하는 mock입니다. 둘째, 환불 플로우가 없습니다. PG사에 환불 요청을 보내는 코드가 없습니다. 이 두 가지가 완성되고, 충분한 부하 테스트를 거쳐야 실제 서비스가 가능합니다. 아키텍처와 주요 기능은 운영 수준으로 설계했지만, 결제와 환불은 Phase 3에서 완성 예정입니다."
