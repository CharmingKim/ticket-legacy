<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticketlegacy.repository.ReviewMapper">
    <select id="findByPerformanceId" resultType="com.ticketlegacy.domain.PerformanceReview">
        SELECT r.*, m.name as member_name
        FROM performance_review r
        JOIN member m ON r.member_id = m.member_id
        WHERE r.performance_id = #{performanceId}
        ORDER BY r.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <select id="countByPerformanceId" resultType="int">
        SELECT COUNT(*) FROM performance_review WHERE performance_id = #{performanceId}
    </select>
    <insert id="insert" parameterType="com.ticketlegacy.domain.PerformanceReview" useGeneratedKeys="true" keyProperty="reviewId">
        INSERT INTO performance_review (performance_id, member_id, rating, content)
        VALUES (#{performanceId}, #{memberId}, #{rating}, #{content})
    </insert>
    <delete id="delete">
        DELETE FROM performance_review WHERE review_id = #{reviewId} AND member_id = #{memberId}
    </delete>
    <update id="update">
        UPDATE performance_review SET rating = #{rating}, content = #{content}, updated_at = NOW()
        WHERE review_id = #{reviewId} AND member_id = #{memberId}
    </update>
    <select id="getAverageRating" resultType="java.lang.Double">
        SELECT AVG(rating) FROM performance_review WHERE performance_id = #{performanceId}
    </select>
    <select id="findByMemberId" resultType="com.ticketlegacy.domain.PerformanceReview">
        SELECT r.*, p.title as performance_title
        FROM performance_review r
        JOIN performance p ON r.performance_id = p.performance_id
        WHERE r.member_id = #{memberId}
        ORDER BY r.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <select id="countByMemberId" resultType="int">
        SELECT COUNT(*) FROM performance_review WHERE member_id = #{memberId}
    </select>
</mapper>
