# TicketLegacy 발표 대본
> ticketlegacy_final.pptx 기준 · 슬라이드 31장

---

## 슬라이드 1 — 타이틀

안녕하세요. TicketLegacy 프로젝트 발표를 시작하겠습니다.

TicketLegacy는 인터파크, 예스24 같은 실제 티켓 예매 서비스를 모델로, Spring Legacy MVC 환경에서 엔터프라이즈 수준의 아키텍처를 직접 설계하고 구현한 프로젝트입니다.

세 개의 독립 포털, 45개 DB 테이블, 공연 데이터 200건 이상, FSM으로 관리되는 도메인 4개입니다.

---

## 슬라이드 2 — 목차

발표는 7개 파트로 구성됩니다. 프로젝트 개요, 요구사항 명세, 시스템 아키텍처, 핵심 기능 구현, 데이터와 성과, 기술적 도전과 해결, 향후 계획 순입니다.

---

## 슬라이드 3 — PART 01: 프로젝트 개요

---

## 슬라이드 4 — 프로젝트 배경 & 목표

이 프로젝트를 시작한 이유는 하나입니다. "동시에 수백 명이 같은 좌석을 선택할 때 중복 예매가 발생하지 않는가"라는 질문이었습니다.

단순 CRUD를 나열하는 수준이 아니라, 실제 서비스의 핵심 문제를 Spring Legacy 환경에서 해결하는 것이 목표였습니다.

핵심 도전 과제는 두 가지였습니다. 첫째, 동시 좌석 예매 시 중복 예매 방지 — Redis Lua 스크립트와 DB 조건부 UPDATE를 조합한 이중 방어선으로 해결했습니다. 둘째, 공연 등록부터 게시까지의 워크플로우를 안전하게 관리하기 위해 FSM, 유한 상태 기계를 설계했습니다.

---

## 슬라이드 5 — 개발 환경 & 기술 제약사항

중요한 제약이 있었습니다. Java 11과 Spring MVC 5.3을 사용했는데, Spring Boot가 아닌 레거시 방식입니다. `jakarta.*`가 아닌 반드시 `javax.*`를 유지해야 했고, 모든 빈 설정을 XML로 직접 작성했습니다.

MySQL 8 단일 스키마를 세 개 WAR이 공유하고, 각 모듈은 독립된 HikariCP 커넥션 풀을 사용합니다.

인증은 JWT HS256 방식으로, 세 포털이 동일한 시크릿 키를 공유하되 쿠키명을 USER_TOKEN, PARTNER_TOKEN, ADMIN_TOKEN으로 분리해 로컬 개발 시 쿠키 충돌 문제를 방지했습니다.

---

## 슬라이드 6 — PART 02: 요구사항 명세

---

## 슬라이드 7 — 기능 요구사항 — 포털별 분류

포털은 역할에 따라 세 개로 분리됩니다.

**ticket-user (8080)**: 일반 회원 포털입니다. 공연 탐색, 좌석 선택과 예매, 대기열, 결제와 환불, 쿠폰, 위시리스트, 취소 대기, 리뷰, 디지털 티켓, 고객 지원까지 실제 서비스의 전체 사용자 플로우를 구현했습니다.

**ticket-partner (8081)**: 기획사와 공연장 담당자 포털입니다. 공연 등록 6단계 워크플로우, 판매 리포트, 정산 조회, 입장 체크인 기능을 제공합니다.

**ticket-admin (8082)**: 운영자 포털입니다. 회원·기획사·공연장담당자 상태 관리, 공연 심사 워크플로우, 정산 관리를 담당합니다.

---

## 슬라이드 8 — 구현 기능 전체 현황

소스코드 기준으로 실제 구현된 기능 전체입니다.

ticket-user 기준으로 보면, 대기열부터 결제 멱등성, 날짜 기반 환불 수수료, 쿠폰 복구, 위시리스트, 취소 대기, 리뷰, 디지털 티켓까지 — 단순 예매 앱이 아니라 실제 티켓팅 서비스의 핵심 기능을 전부 구현했습니다.

ticket-partner는 기획사의 공연 등록 워크플로우 6단계와 공연장 담당자의 입장 체크인이 핵심입니다.

ticket-admin은 SUPER_ADMIN이 기획사 승인부터 공연 심사, 좌석 설정, 정산까지, STAFF는 예매 조회와 강제 취소 권한을 갖습니다.

---

## 슬라이드 9 — 기능 요구사항 — 좌석 예매 상세 플로우

좌석 예매 플로우는 6단계입니다.

좌석 선택 → Redis 원자 선점 → 결제 요청 → 예약 확정 → 타임아웃 처리 → 취소 환불 순서입니다.

Redis 선점 단계가 핵심입니다. Lua 스크립트로 AVAILABLE 확인과 HOLD 전환을 원자적으로 처리합니다. TTL은 10분이며, @Scheduled로 3초마다 만료된 HOLD를 AVAILABLE로 복원합니다.

---

## 슬라이드 10 — 비기능 요구사항

보안: JWT HttpOnly 쿠키로 XSS를 방어하고, BCrypt 해싱으로 비밀번호를 저장합니다. 역할 기반 접근 제어(RBAC)를 적용했습니다.

동시성: Redis Lua 원자 연산과 DB 조건부 UPDATE 조합으로 Race Condition을 완전히 방지했습니다.

멱등성: Payment 테이블의 `idempotency_key UNIQUE` 제약으로 네트워크 재시도 시 중복 결제를 원천 차단합니다.

가용성: Redis 장애 시 DB-only 모드로 graceful 강등됩니다.

---

## 슬라이드 11 — PART 03: 시스템 아키텍처

---

## 슬라이드 12 — 시스템 아키텍처

전체 아키텍처는 Maven 멀티모듈 구조입니다. ticket-common 공유 JAR 하나를 세 개의 WAR이 의존합니다.

ticket-common에는 도메인 20개, 서비스 15개, Mapper XML 27개가 포함됩니다. 공통 비즈니스 로직을 전부 여기에 집중시켜 중복을 제거했습니다.

세 포털은 서로 직접 의존하지 않습니다. DB와 JWT를 통해서만 간접적으로 연결됩니다.

---

## 슬라이드 13 — Maven 멀티모듈 구조

ticket-common은 외부 의존성만 가집니다. 세 WAR은 ticket-common에만 단방향으로 의존합니다. WAR 간 순환 의존은 구조적으로 불가능합니다.

이 구조 덕분에 ticket-common의 서비스 로직을 수정하면 세 WAR 모두에 즉시 반영됩니다.

---

## 슬라이드 14 — 기술 스택

Spring MVC 5.3과 Tomcat 9를 사용합니다. 모든 설정은 XML 기반으로, root-context, servlet-context, security-context 세 파일로 관리합니다.

데이터 계층은 MyBatis 3.5와 MySQL 8의 조합입니다. 동적 SQL과 resultMap을 적극 활용해 45개 테이블을 처리합니다.

캐시는 Redis Lettuce 클라이언트, 인증은 JJWT 0.11.5로 HS256 서명을 처리합니다.

---

## 슬라이드 15 — 핵심 도메인 ER 다이어그램 ⭐

ER 다이어그램을 보시면 도메인이 크게 6개 영역으로 구분됩니다.

**회원 도메인**: member를 중심으로 FSM으로 관리되는 상태가 있습니다. ACTIVE, SUSPENDED, DELETED로 전이되며, 탈퇴한 회원은 절대 복구되지 않습니다.

**파트너 도메인**: promoter와 venue_manager 모두 member와 1:1 관계를 가집니다. 승인 여부도 FSM으로 관리됩니다.

**공연 도메인**: performance → schedule → seat_inventory 순으로 계층 구조를 가집니다.

**핵심 관계**: seat_inventory가 Redis 선점과 DB 조건부 UPDATE의 대상 테이블입니다. payment의 idempotency_key UNIQUE 제약이 중복 결제 차단의 핵심입니다.

---

## 슬라이드 16 — 핵심 DB 테이블

총 45개 테이블 중 28개를 실제로 사용합니다. 공연 관련이 8개로 가장 많고, 결제와 쿠폰 7개, 좌석과 예매 6개 순입니다.

나머지 17개는 포인트, 알림, 약관 등 Phase 4 이후 구현 예정 기능입니다. 전체 도메인을 설계 단계에서 미리 정의하고, 우선순위에 따라 핵심 기능부터 구현했습니다.

---

## 슬라이드 17 — PART 04: 핵심 기능 구현

---

## 슬라이드 18 — 좌석 예매 동시성 제어 — Dual Defense ⭐

이 부분이 프로젝트의 기술적 핵심입니다.

동시에 여러 사용자가 같은 좌석을 선택하면 어떻게 될까요? 일반적인 DB 트랜잭션만으로는 Race Condition을 완전히 막을 수 없습니다.

**1단계: Redis Lua 스크립트**. AVAILABLE 상태 확인과 HOLD 전환을 Lua 스크립트 한 번의 호출로 처리합니다. Redis는 단일 스레드이므로 이 구간에서 원자성이 보장됩니다. 동시에 100명이 같은 좌석을 선택해도 단 1명만 HOLD에 성공합니다.

**2단계: DB 조건부 UPDATE**. `WHERE status = 'AVAILABLE'` 조건을 붙인 UPDATE로, Redis가 장애 상태일 때도 DB 레벨에서 이중 검증합니다.

Redis 장애 시 자동으로 DB-only 모드로 강등되어 서비스가 중단되지 않습니다.

---

## 슬라이드 19 — FSM — 상태 전환 다이어그램 ⭐

잘못된 상태 전이를 코드 레벨에서 원천 차단합니다.

**공연(Performance) FSM**: DRAFT에서 시작합니다. 파트너가 심사 요청하면 SUBMITTED, 어드민이 검토하면 REVIEW, 승인 후 APPROVED, 게시하면 ON_SALE이 됩니다. 반려 시 REJECTED로 전환됩니다. 심사 안 된 공연이 ON_SALE로 바로 이동하는 것은 코드상 불가능합니다.

**회원 상태 FSM**: ACTIVE → SUSPENDED → DELETED. 탈퇴한 회원이 다시 ACTIVE가 되는 버그를 원천 차단합니다.

**기획사, 공연장담당자 FSM**: PENDING → APPROVED/REJECTED. 승인 없이 파트너 기능을 사용하는 것을 차단합니다.

FSM이 없으면 이런 버그들이 실제 서비스에서 조용히 발생합니다.

---

## 슬라이드 20 — 인증 & 보안 아키텍처

인증 플로우는 5단계입니다.

로그인 요청 → JWT 발급(HS256, 2시간 만료) → HttpOnly 쿠키 저장 → 이후 요청에서 자동 전송 → JwtAuthenticationFilter가 SecurityContext 설정.

포털별로 쿠키명을 분리한 것이 포인트입니다. USER_TOKEN, PARTNER_TOKEN, ADMIN_TOKEN으로 분리해 로컬 개발 시 같은 localhost에서 세 서버를 동시에 띄워도 쿠키가 덮어써지지 않습니다.

접근 제어는 Spring Security의 URL 패턴 기반입니다. `/backoffice/super/**`는 SUPER_ADMIN만, `/partner/promoter/**`는 PROMOTER 역할만 접근 가능합니다.

---

## 슬라이드 21 — 결제 멱등성 & 날짜 기반 환불 수수료 ⭐

단순 결제 구현이 아닙니다. 실제 서비스에서 발생하는 두 가지 문제를 해결했습니다.

**결제 멱등성**: 네트워크 오류로 "결제하기"를 두 번 클릭하면 중복 청구가 발생합니다. `idempotency_key UNIQUE` 제약을 payment 테이블에 적용해 동일 요청이 두 번 들어와도 INSERT가 한 번만 실행됩니다. 결제 실패 시 고아가 된 PENDING 예약은 자동으로 정리됩니다.

**날짜 기반 환불 수수료**: 관람일 10일 전은 전액 환불, 7~9일 전은 수수료 10%, 3~6일 전은 20%, 1~2일 전은 30%, 당일 이후는 환불 불가입니다. 쿠폰을 사용한 결제를 전액 환불할 때는 사용한 쿠폰이 자동으로 복구됩니다.

이 두 가지는 실제 티켓팅 서비스에서 반드시 필요한 기능인데, 교육용 프로젝트에서는 잘 다루지 않는 부분입니다.

---

## 슬라이드 22 — Elasticsearch 검색 엔진 — 설계 & 구현 완료 ⭐

코드 레벨에서는 구현이 완료되어 있습니다.

**동의어 사전**: "아이유"로 검색하면 "IU", "이지은"도 함께 검색됩니다. "BTS"로 검색하면 "방탄소년단"도 매칭됩니다. 코드에 동의어 사전이 직접 구현되어 있습니다.

**오타 교정**: Fuzziness.AUTO를 적용해 첫 글자부터 오타를 허용합니다.

**하이라이팅**: 검색 결과에서 일치 키워드가 강조 태그로 감싸져 반환됩니다.

**자동완성**: 2글자 이상 입력 시 제목 기반 추천어 최대 10개를 반환합니다.

인덱싱은 공연 등록 시 `@Async`로 비동기 호출되어 메인 트랜잭션 지연이 없습니다. 전체 재인덱싱은 `/api/search/reindex` 한 번으로 가능합니다.

현재 배포 환경에서는 t3.micro 메모리 제약으로 ES 서버를 올리지 않고 DB LIKE 검색으로 fallback 동작 중입니다. Phase 4에서 AWS OpenSearch 연동 예정입니다.

---

## 슬라이드 23 — PART 05: 데이터 & 성과

---

## 슬라이드 24 — 데이터셋 구성 & E2E 데모 플로우

데이터셋은 세 단계로 구성됩니다. schema_total.sql로 45개 테이블 스키마를 생성하고, data_total.sql로 계정과 기본 데이터를 주입합니다. data_extension.sql은 `WITH RECURSIVE`를 활용해 공연 214건, 좌석 인벤토리 12,840개를 자동 생성합니다.

E2E 데모 플로우는 실제 서비스 운영 흐름을 따릅니다.

1단계: 기획사가 파트너 포털에서 공연을 등록합니다.
2단계: 어드민이 심사하고 승인 후 게시합니다.
3단계: 사용자가 공연을 검색하고 좌석을 선택합니다.
4단계: Redis Lua 원자 선점 후 결제합니다.
5단계: 마이페이지에서 예매 확인 및 취소를 합니다.

이 전체 플로우가 실제 배포 환경에서 동작함을 확인했습니다.

---

## 슬라이드 25 — 배포 아키텍처 — AWS EC2 + Docker + RDS ⭐

이 프로젝트는 실제 AWS에 배포되어 현재 운영 중입니다.

EC2 t3.micro 인스턴스 위에 Docker Compose로 4개 컨테이너를 운영합니다. ticket-user, ticket-partner, ticket-admin 세 개의 Tomcat WAR 컨테이너가 각각 8080, 8081, 8082 포트에 바인딩됩니다. Redis 컨테이너는 메모리 50MB로 제한해 자원을 효율적으로 사용합니다.

t3.micro는 RAM이 1GB밖에 되지 않습니다. 각 컨테이너의 JVM Heap을 `-Xmx220m` 수준으로 제한하고, EC2에 2GB Swap을 추가해 안정성을 확보했습니다.

MySQL은 Amazon RDS db.t3.micro로 분리했습니다. 애플리케이션과 DB를 분리함으로써 EC2 메모리 부족 문제를 해결했습니다.

DB URL, 인증 정보, JWT 시크릿 키는 모두 환경변수로 주입합니다. 코드에 하드코딩된 시크릿은 없습니다.

---

## 슬라이드 26 — PART 06: 기술적 도전 & 해결

---

## 슬라이드 27 — 기술적 도전 & 해결

개발 과정에서 겪은 실제 이슈 4가지를 공유합니다.

**첫 번째: JSP EL vs JS 템플릿 리터럴 충돌**. JavaScript에서 `${item.name}` 같은 템플릿 리터럴을 사용했는데, JSP가 서버사이드에서 먼저 EL로 파싱해 'false'라는 문자열로 치환하는 문제가 발생했습니다. JS 문자열 연결 방식으로 전면 교체해 해결했습니다.

**두 번째: BCrypt 해시 불일치**. 여러 SQL 파일에 분산된 초기 데이터가 서로 다른 BCrypt 해시를 사용해 모든 계정의 로그인이 불가능해지는 사태가 발생했습니다. schema_total.sql과 data_total.sql로 단일화하고 해시를 일괄 교체해 해결했습니다.

**세 번째: Redis 장애 시 서버 기동 실패**. LettuceConnectionFactory가 Redis 연결 실패 시 즉시 예외를 던져 서버 자체가 뜨지 않는 문제였습니다. Graceful 강등 로직을 구현해 Redis 불가 시 DB-only 모드로 자동 전환되도록 했습니다.

**네 번째: Mapper 직접 주입 문제**. 컨트롤러에서 Mapper를 직접 주입한 코드가 있었습니다. 트랜잭션 경계가 누락되는 문제였습니다. 전체 매퍼를 Service 계층 메서드로 이전하고 @Transactional을 적용했습니다.

---

## 슬라이드 28 — PART 07: 향후 계획 & 결론

---

## 슬라이드 29 — 향후 개발 계획 (Phase 3~6)

현재 구현된 기능을 바탕으로 향후 4개 Phase를 계획했습니다.

**Phase 3**: 예매 동시성 강화입니다. Redis와 DB 정합성을 더욱 완전하게 확보하고, 환불 엣지 케이스 처리를 추가할 계획입니다.

**Phase 4**: Elasticsearch 완전 연동입니다. 현재 코드 구현은 완료된 상태이며, AWS OpenSearch Service를 연동하고 Edge Ngram 자동완성 인덱스를 구성할 계획입니다.

**Phase 5**: 코드 품질 정리입니다. 중복 유틸 제거, enum 정리 등 기술 부채를 해소합니다.

**Phase 6**: 운영 모니터링입니다. 포인트, 알림, QR 스캔 입장 시스템, 시스템 메트릭 대시보드를 추가할 예정입니다.

---

## 슬라이드 30 — 결론

TicketLegacy를 한 줄로 요약하면 이렇습니다.

"Spring Legacy MVC 5.3 기반으로 엔터프라이즈 수준의 공연 예매 플랫폼을 완전히 구현했다."

28개 테이블 실사용, 200건 이상의 공연 데이터, 3개의 독립 WAR 포털, 4개 FSM 도메인, Dual Defense 동시성 제어, 결제 멱등성, 날짜 기반 환불, Elasticsearch 설계, AWS Docker 배포까지.

단순히 기능이 동작하는 수준이 아니라, 실제 운영 환경에서 안 터지는 코드를 목표로 했습니다. 트랜잭션 경계, 동시성, 멱등성, 보안 — 모든 변경에서 이 4가지를 검토했습니다.

이상으로 발표를 마치겠습니다. 감사합니다.

---

## 슬라이드 31 — Q&A

**예상 질문 & 답변**

**Q. Redis가 없으면 서비스가 중단되나요?**
A. 아닙니다. Redis 장애 시 자동으로 DB-only 모드로 강등됩니다. 좌석 선점의 1차 방어선(Redis Lua)을 건너뛰고 2차 방어선(DB 조건부 UPDATE)만으로 동작합니다. 성능은 떨어지지만 서비스는 유지됩니다.

**Q. Spring Boot를 쓰지 않고 Spring Legacy를 선택한 이유는?**
A. 레거시 시스템은 실제 금융, 공공기관 등 대형 시스템에서 여전히 광범위하게 사용됩니다. XML 기반 설정을 직접 다루는 능력을 기르고, Boot가 추상화하는 부분을 직접 구성하면서 Spring의 내부 동작 원리를 더 깊이 이해할 수 있었습니다.

**Q. Elasticsearch가 연결 안 된 상태에서 검색이 동작하나요?**
A. 네. ES 연결 실패 시 DB LIKE 검색으로 자동 fallback됩니다. 214건의 공연 데이터 규모에서는 LIKE 검색도 충분히 빠릅니다. ES는 코드 구현이 완료된 상태이며, OpenSearch 연동 후 자동으로 ES 검색으로 업그레이드됩니다.

**Q. 결제 중복 방지를 어떻게 구현했나요?**
A. payment 테이블에 idempotency_key UNIQUE 제약을 걸었습니다. 결제 요청마다 고유한 키를 생성해 INSERT 시 DB가 중복을 차단합니다. 네트워크 오류로 재시도해도 두 번 청구되지 않습니다.

**Q. 동시성 테스트는 어떻게 진행했나요?**
A. 코드 레벨에서 Race Condition 시나리오를 분석하고, Lua 스크립트의 원자성과 DB의 조건부 UPDATE로 이를 방어했습니다. Redis의 단일 스레드 특성이 원자성을 보장하는 핵심입니다.
